---
name: audit-2026-08-13-full
description: 2026-08-13 five-worker full audit — red main is a test time-bomb (stale_gauntlet clock, ~2-line fix); gauntlet ADMIT; quality/engine/consumption/research headlines; upstream rolling-URL checksum drift breaks fetch:sources
metadata:
  type: project
  modified: 2026-08-17T20:47:19.009Z
---

2026-08-13 five-worker audit of jestek-dev/scripture-search-engine at origin/main HEAD 5033517 (PR #20 merge). Read-only; full report published as an artifact ("Scripture Search Engine — Full Audit & Strengthening Plan (2026-08-13)").

**Main red cause (the only blocker):** `workbench/src/admission.ts:606-609` rejects any gauntlet report whose finishedAt is >24h before the REAL clock; `workbench/test/admission.test.ts:159-160,197` hard-code `2026-08-11T10:00:00Z` fixture timestamps; the fake clock (test line 264) is injected only into `runAdmission`'s deps, NOT into `previewAdmission` (which defaults to `new Date()`, admission.ts:558). So 15 tests in admission.test.ts fail on BOTH OSes on every run since 2026-08-12T10:00Z, forever, regardless of commit content (PR #20's docs-only merge went red for this reason). Fix is ~2 lines: inject the test clock into `previewAdmission` (or derive fixture timestamps from now); do NOT widen the 24h freshness window.

**Gauntlet at HEAD:** verdict ADMIT, all 11 gates pass (clean worktree; G3 70 fixtures/58 concepts covered, G8 0% churn, G10 131 MiB/160, G11 p95 8.9ms). Typecheck clean. Everything except the time-bomb is healthy.

**Quality-probe headline:** concept-covered queries excellent; zero misattributions in ~15 explanation spot checks; no theology-adjudicating language. But sense-inverted #1s wherever coverage is absent: "new beginnings"→Eccl 1:9 ("no new thing"), "comforter"→Job 16:2 ("miserable comforters"), "it is well with my soul"→Jer 4:10, "lord's supper"→1 Cor 11:20 (the denial verse). `peace-of-god.yaml` double-lists 1 Pet 5:7 (torrey+editorial) and the engine SUMS duplicate anchors (35.2 pts beats the weight-1.0 Phil 4:6-7) — no gate catches within-concept duplicate refs. The 2026-08-08 bare-single-token lexicon pass was applied unevenly: holiness/healing/temptation concepts exist but are unreachable by the bare word (1 Cor 10:13 unreachable for "temptation") — single largest quick win. Missing concepts by pastoral importance: Holy Spirit/comforter/helper (WEB says "Counselor" so lexically unreachable too), baptism, comfort (2 Cor 1:3-4, Matt 5:4), new creation, cornerstone, doubt. ~70% of weakness is data, ~30% engine (exact_phrase +60 drowns concept_anchor ≤28 on short phrases; translation_variant as sole evidence promotes junk; flat passage_terms ties; rank-adjacency anchor collapse misses Ps 150 filling 5 slots).

**Engine-audit headline:** all covenants hold in code (no I/O, one tokenizer, no theology scores, explanations = scoring evidence). But: G6 is a hardcoded `pass(...)` at `eval/src/gauntlet.ts:705-709` (only gate that reports pass without running anything); the eval system asserts presence-in-windows NEVER rank (80 assertions top-10, 12 top-5, 0 top-1/top-3, 0 preferredOrder uses) — a #1→#8 regression passes everything; ENGINE_VERSION bump discipline is social not mechanical (no gate diffs ordering keyed to version); CI is V8/Node-24 ubuntu+windows only while consumers are Hermes/JSC React-Native apps (Math.log ulp drift + unpinned SQLite build are real cross-platform hazards CI cannot see); cross-OS CI comparison is set/summary-level, not order-level. Perf: full artifact p50 18.5ms, p95 44.5ms, ~30MB RSS delta. Top fixes: ordered per-probe snapshot in machine report (makes cross-OS order-exact AND version-bump mechanical), property-based budget/determinism tests (makes G6 real), rank-sensitive fixtures + nDCG (also implements the promised-but-unimplemented NO MEASURABLE EFFECT verdict), gate mutation tests, consumer-runtime conformance kit.

**Consumption headline:** committed descriptor at HEAD says databaseSha256 `35b7a6f3…` (137,412,608 bytes, schema 6, 33 concepts, engineVersion 0.7.1, stale/blocksRelease) — matches NO published asset; workbench fetch-artifact fails with sha256 MISMATCH. npm latest engine = 0.7.1, supports schemas 1–5 only, would REJECT the current schema-6 artifact; 0.8.0/0.9.0 unpublished/untagged (0.8.0 was breaking). v0.7.1 release pair is self-consistent (`b57d36…`) but only by accident of the vacuous pre-fix release.yml. Repair: mint descriptor from CI (workflow_dispatch, never a laptop — a locally-built descriptor recreates the defect, NEEDS-JESSE §1.6e/§1.9), persist+attest the canonical bytes and promote those exact bytes at tag time (never rebuild), decouple artifact identity from engineVersion, add a post-release smoke test. Licensing: unusually clean, but the OpenBible CC BY attribution passthrough obligation on Maskil/Setlist/Versed is undocumented in the §5 consumer contract, and the ESV/NIV/NLT-derived verse_translation_tokens index deserves one counsel pass before schema-6 ships to commercial apps.

**Research headline (ranked by value ÷ review time):** (1) OpenBible pericope section counts (CC BY, tiny TSV) → pericope grouping/range collapse; (2) STEPBible TVTMS (versification correctness — silent wrong-verse insurance) + TIPNR (proper-noun disambiguation, CC BY 4.0); (3) BSB translation tables (PD-pending-confirmation of aux files) → Strong's/lemma pivot; (4) Robertson's Harmony 1922 (PD) or xref-vote-derived parallels table → synoptic dedup; (5) Hymnary.org scripture index — NOT open (© Harry Plantinga/Calvin), needs a permission email, highest worship-leader value per row; start the email early. Skip: Thompson chains (no PD transcription), MH Concise (NO MEASURABLE EFFECT expected), Strong's glosses (subsumed). Techniques (all deterministic, ENGINE_VERSION-bump territory): pericope grouping, SymSpell-style precomputed spelling correction over the closed vocab, positional phrase/proximity scoring, bounded xref re-rank at result time, BM25F formalization of the existing channels+caps. Eval upgrade: graded qrels + nDCG@10/Recall@50/MRR + second-judge Cohen's κ (≥0.6) on a 10-15% sample; no usable public benchmark exists — publishing the fixture set would BE the benchmark.

**Also:** upstream rolling-URL checksum drift currently breaks `fetch:sources` at HEAD — all three rolling sources changed bytes (WEB engwebp_vpl.zip `3458ca34…`→`3073fead…`; openbible-topics `2239700d…`→`96bf2893…`; openbible-xrefs `36d1b198…`→`78c92682…`). Manifests need reviewed re-pins via PR + gauntlet re-admission (the WEB TEXT itself changed → corpusFingerprint moves, fixtures need review). The audit's own uncommitted fetch/build leftovers were recorded and then discarded (`git restore .`) — deliberately NOT committed.

Related: [[workbench-v25-audit]], [[release-v071-hash-defect]], [[pr20-one-click-plan-audit]], [[concordance-assessment]]

2026-08-14: AAA implementation plan built from this audit (11 items, each critic-approved 9/10; final review 9/10). Committed to branch claude/hearth-thread-vvwdi2 as docs/plans/2026-08-14-implementation-plan.md (commit 23a4e4f9af9051058b669e23686804d7bead5dad).

2026-08-17: Phase 0 of the implementation plan fully landed on main via PRs #21 (admission-test clock fix), #23 (v1 judgment endpoint retired, compiler unbrickable), #24 (evidence-based not-relevant flow; note: CI runs a Playwright browser suite that root verify does NOT — e2e mocks must know every boot-time fetch), #25 (source-drift sentinel + re-pin process), #26 (release repair: CI-minted descriptors via mint-artifact.yml workflow_dispatch with release_tag input, no tag-time rebuilds, release-smoke test), #27 (independent baseline-review governance; approval cutover held for a human signature), #28 (one-click plan doc capped at draft-PR with guard test). All built from staged patches with builder+harsh-critic loops, PRs opened from clean claude/hearth-thread-vvwdi2-prN branches off main because Jesse's Books/ commit c3ff4ba sits on the thread branch. Outstanding Phase-0 human steps: Jesse runs mint-artifact.yml (v0.9.0), reviews the descriptor PR, tags v0.9.0; designates an independent baseline reviewer (g2 cutover patch held in scratchpad). Session-type policy: Claude sessions cannot dispatch new workflow runs (dispatch denied; re-run of existing runs allowed).
