# HANDOFF — scripture-search-engine

> **Refreshed 2026-08-27 — this repo copy (`project-handoff/HANDOFF.md`) is now the canonical HANDOFF; the copy at `/mnt/project-files/HANDOFF.md` is superseded.** The refresh updates the document to main @ `65b6a6f` (the PR #67 squash merge, 2026-08-27 13:44 UTC): identity **0.14.0 / corpus `6450b7d7…` / layer `9a11fd56…`**, concept census **288**, **530 active golden fixtures**, **J39 signing fully unblocked**. The 2026-08-25/26 body below is kept intact per this document's own convention (dated addenda, stamped claims never rewritten); read the **"Update addendum, 2026-08-27"** at the end first for current state, and note the inline **[Superseded 2026-08-27 →]** markers where the body's snapshot facts have moved. The complete outstanding checklist now lives in `project-handoff/OUTSTANDING.md`; the project history in `project-handoff/HISTORY.md`.

**Written 2026-08-25; last re-verified 2026-08-25 ~19:45 UTC; refreshed 2026-08-27 (see banner above).** Verified against `origin/main` @ `caf9fe3` (the PR #51 merge — main had not moved at the re-verification), GitHub state as of 2026-08-25 19:45 UTC, and the files under `/mnt/project-files`. This document is the single entry point to the project's state; it assumes no other context. Point-in-time facts (open PRs, branch counts, latest CI runs) are stamped with their observation time — treat them as snapshots, not standing truths. Repo: https://github.com/jestek-dev/scripture-search-engine (public).

---

## First hour back — ordered checklist

> **[Superseded 2026-08-27 →]** Steps 1, 3, and 6 are DONE (their dated notes below record it), and step 2/4's fingerprints have moved twice since — the J39 records must bind **0.14.0 / `6450b7d7…` / `9a11fd56…`** (post-#67), not the `644b241c…`/`b24ea16d…` values quoted below. The current ordered path is in the 2026-08-27 addendum: **J39 sign once → branch cleanup → v0.14.0 mint** (mint blockers: supplement ruling S1–S6, the G10 size call, then PR #61 post-mint).

1. **Review and merge PR #52** (small, self-contained bug fix; draft awaiting you): https://github.com/jestek-dev/scripture-search-engine/pull/52 — fixes the `compileJudgments` crash on verse-level `web-subset.json` entries. Workbench tooling only; no ordering impact, no engine bump. **DONE 2026-08-26:** merged by jestek-dev at 01:22 UTC; squash merge commit `031ed69` (`031ed69b5c4be84bdf4bd3b03e9b589891ad82bf`), now main's HEAD.
2. **J39 — designate the independent baseline reviewer now** (process: `docs/governance/probe-baseline-review.md`), **but schedule the sign-off for after step 3's re-pin merges.** Draft PR #53 recommends this ordering ("Merge this PR first, then sign J39 once"), and the argument holds: the approval records bind the exact identity triple, and the re-pin moves `corpusFingerprint` (`d2846ff3…` → `644b241c…`) and `layerFingerprint` (`954f04dc…` → `2b85fa88…`) — approvals signed against today's main would be invalidated the day PR #53 lands, forcing the reviewer to sign a second, chained set inside the re-pin flow. Signing **once**, against the post-re-pin identity, is less process and less rubber-stamp risk (the plan's own caution on J39: real eyes, not a rubber stamp). Designation itself need not wait — finding the reviewer is the slow part. Details in [item (a)](#a-j39--stale-baseline-approval-records).
3. **J52 — finish the errand's human steps, then review and merge draft PR #53** (https://github.com/jestek-dev/scripture-search-engine/pull/53, opened 2026-08-25 19:21 UTC — a one-PR re-pin of all three drifted sources that **supersedes the two staged runbooks as the executable path**; they remain the background/method docs). Human steps first, in PR #53's stated order: create the `source-snapshots-2026-08` release and upload the captured bytes preserved on branch `claude/source-snapshots-2026-08-staging` (commands in that branch's README, from `docs/source-snapshots-errand.md` steps 2–3), verify the asset digests, and run the A5b archive search for the old WEB bytes (`3458ca34…`) from an unrestricted machine — **PR #53 must not merge until the release assets exist with verified digests**, and nothing in it signs off the A5b loss (that call is yours alone). Review = confirm the new pins and the delta reports; also read its FINDING (the openbible-subset/passage-terms catch-up is deliberately excluded and needs its own follow-up ruling). Merging unblocks the battery legs' source fetch and gives the weekly `sources.yml` alarm its first green run — **transiently**: each subsequent upstream roll honestly re-fires it (see [item (b)](#b-j52--upstream-source-drift-three-stale-pins)). It does **not** turn the battery CI jobs green: the build leg advances past the fetch step and fails at "Committed descriptor matches the rebuilt artifact" (`gauntlet.yml`), because the committed descriptor is still the stale v0.7.1 phantom — observed, not just predicted: PR #53's own CI run 32889151337 (completed 2026-08-25 19:40 UTC) already shows exactly this shape on the PR branch (item (b)). **DONE 2026-08-26:** the human steps completed and the PR merged. The `source-snapshots-2026-08` release was published by jestek-dev at 14:47 UTC, and PR #53's updated body records the digest verification (all three assets match the staging `CHECKSUMS.txt`; `check:drift` on the merged tree ends "Every reachable pin matches what upstream serves"); jestek-dev then merged PR #53 at 15:22 UTC — squash commit `e762d1c`, main's new HEAD. Two deltas from the plan as written above: the concept-pack merges (#54, #60) landed on main first and were merged into the PR branch before its merge, so the post-re-pin **layerFingerprint is `b24ea16d…`, not the `2b85fa88…` quoted above** (corpusFingerprint `644b241c…` and engine 0.14.0 as stated) — the J39 approvals in steps 2 and 4 must bind `0.14.0` / `644b241c…` / `b24ea16d…`, with `priorProvenance` chaining the updated merge-base blobs at main @ `6367855` (`ordering.snapshot.json` @ blob `f534b863…`, `probes.json` @ blob `8b9d774a…`, per the PR body's updated J39-interplay section). **A5b remains open** — the merge signs nothing off there.
4. **J39 sign-off (once, post-re-pin):** the designated reviewer reviews the packet and authors the two approval records binding engine 0.14.0 and the post-re-pin fingerprints (`644b241c…` / `2b85fa88…`), chaining `priorProvenance` to the merge-base blobs PR #53 pre-computes in its body (`ordering.snapshot.json` @ blob `0ef79f76…`, `probes.json` @ blob `bbee4834…`). Landing them via a reviewed PR turns both `verify` legs green. (**Corrected 2026-08-26**, post-merge: the layerFingerprint to bind is `b24ea16d…` and the merge-base blobs moved to `f534b863…` / `8b9d774a…` at main @ `6367855` — see step 3's DONE note.)
5. **Once the `verify` legs are green and the pinned sources are fetchable (i.e. after steps 3–4): cut the release — the tag must be `v0.14.0` on today's main, not v0.9.0** — per the [Release runbook](#release-runbook) (mint → descriptor PR → tag push; the runbook explains why the ship-forward docs' "v0.9.0" is stale and what to do if you'd rather re-version). Do not wait for "main green" before this step — it cannot happen first. But know the end state honestly: merging the mint's descriptor PR replaces the stale phantom, yet **the battery jobs still stay red** at the descriptor gate, because `gauntlet.yml`'s rebuild drops the minted `release.tag` field — closing that requires a small workflow fix that does not exist on main yet (a normal PR you review; exact change in [Known defects #5](#known-defects-and-their-state)). **Update 2026-08-26: that fix is now written — draft PR #61** (https://github.com/jestek-dev/scripture-search-engine/pull/61) awaits your review and merge; details in Known defects #5. Green after this step: both `verify` legs, `sources.yml` (until the next upstream roll), the release itself. Red until the workflow fix lands: battery build + cross-leg. Related pre-release decisions: J47 (the tag push itself), J48 (verse_translation_tokens counsel), J51 (immutable releases, default yes). **Caution added 2026-08-26: one more blocker discovered — the v0.14.0 artifact busts the G10 size budget** (167.84 MiB measured vs 160 MiB; `spelling_deletes` and `spelling_terms` sub-budgets also over). The mint run itself passes, but the descriptor PR (runbook step 3) will REJECT on G10 until a reviewed `eval/budgets.json` decision lands — see Known defects #6 for exact numbers, mechanics, and the recommended path (a reviewed budget bump justified by measured growth). Budget PR before or alongside the descriptor PR.
6. **Added 2026-08-26:** Review and merge the six stacked draft concept-pack PRs **#54 → #55 → #56 → #57 → #58 → #59** in that order (retargeting each as its predecessor merges — #54 targets main; each later PR targets the previous one's head branch), and work the backlog file's flagged decisions (`/mnt/project-files/research/bible-rollout/engine-pack-backlog.md`, § "Items flagged for Jesse's decision"). This does **not** gate main-green (the stack is data-only; its CI reds are exactly the pre-existing J39/J52 classes, per each PR's standing-down triage comment), so it sequences after the PR #53 / J39 / mint items above — but it is the disposition of the entire 161-concept rollout and each merge IS the engine-admission event for its batch's packs. Details in the [open-PR inventory](#state-of-main) and [Deferred roadmap](#deferred-roadmap). **Update 2026-08-26 (afternoon): DONE — with a detour.** #54 merged to main at 14:18 UTC (`8c8b78d`), but #55–#59 were merged 14:19–14:21 UTC **without being retargeted**, so each squash commit landed on the previous batch's branch, not main; recovery PR **#60** ("Land batches 2–6 on main", carrying exactly the already-reviewed content of #55–#59, census 168 → 239) was merged by jestek-dev at 14:53 UTC as `6367855`. Main now carries all 239 concepts. The backlog file's flagged decisions remain open.
7. When time allows: work the rest of the [open decisions](#open-decisions-reserved-for-jesse) — especially J61 (the in-copyright-PDFs branch, a required decision) and the PR #33 / PR #41 / PR #51 decision items.

---

## Executive summary

> **[Superseded 2026-08-27 →]** The paragraph below is the 2026-08-25 snapshot. As of 2026-08-27: main is `65b6a6f` with **288** concepts and **530** active golden fixtures; J52 is done (PR #53), the 161-concept rollout, full-Bible fixture corpus (31,098 verses, PR #64), popular-verses guards (#63), alias batch 1 (#65), and the 50-row roster build (#67) are all merged; the only open PR is draft #61. The remaining red classes are exactly J39 (unblocked, awaiting the signature) and the battery descriptor gate (cleared by the mint + #61).

`scripture-search-engine` is a deterministic, concept-aware Scripture search engine shared by three consumer apps (Maskil, LH Worship Setlist, Versed); its covenant (in `CLAUDE.md`) is **no AI at runtime**, byte-identical ordering across platforms, one tokenizer, explanations as part of the contract, and no theology scoring. As of 2026-08-25, main (`caf9fe3`) carries engine **0.14.0** with **153** curated concepts, a full evaluation gauntlet, a search-first web dashboard ("The Study") served at `/`, and a promote-only release pipeline — but **npm still serves 0.7.1**, because no release has been cut since 2026-07-31. Main's CI has been red since 2026-08-20 for exactly two known, fully diagnosed reasons: stale baseline-approval records awaiting an independent reviewer (**J39**) and drifted upstream source pins awaiting a re-pin (**J52**); a third, currently latent battery failure (the stale committed descriptor) surfaces once the sources are fetchable, and clearing *it* takes the next mint's descriptor PR **plus a small `gauntlet.yml` fix that is not yet written** (the battery rebuild drops the minted `release.tag`, so the descriptor gate cannot pass as the workflow stands — Known defects #5). Everything else needed is written down and staged. Two draft PRs are open as of 2026-08-25 19:45 UTC: **#52** (workbench tooling crash fix) and **#53** (the J52 re-pin of all three sources, blocked on one human upload step); there are no other open PRs. Note: the next release tag is **v0.14.0**, not the "v0.9.0" older docs name — the tree re-versioned past that number (Release runbook). The large remaining backlog is not code but **decisions reserved for Jesse** — a 70-item approval registry (Appendix A of `/mnt/project-files/plans/2026-08-20-implementation-plan.md`), decision tables in the bodies of PRs #33, #41, and #51, and the disposition of one branch that publicly serves in-copyright PDFs (J61).

---

## State of main

> **[Superseded 2026-08-27 →]** HEAD is now **`65b6a6f`** (PR #67, merged 2026-08-27 13:44 UTC); census **288**; identity **0.14.0 / `6450b7d7…` / `9a11fd56…`**; open PRs: exactly one, draft #61. Full sequence in the 2026-08-27 addendum.

- **HEAD:** `caf9fe3` — "Apologetics concept packs: 20 packs/extensions, fixtures-first, all ADMIT (#51)", merged by jestek-dev 2026-08-25 18:06 UTC; still HEAD at 19:45 UTC. **Open PRs as of 2026-08-25 19:45 UTC: exactly two, both drafts** — #52 (tooling fix) and #53 (the J52 re-pin; see the CI section). **Update 2026-08-26:** main advanced to `031ed69` ("Fix compileJudgments crash on verse-level web-subset selections (#52)") via PR #52's squash merge at 01:22 UTC; open PRs are now exactly one — draft #53 (verified via the GitHub API and a fresh `git fetch origin main`). **Second update 2026-08-26 ~10:55 UTC:** open PRs are now exactly **seven, all drafts** — #53 (unchanged: base `main`, head `09e2794`) plus the six-PR stacked concept-pack train **#54 → #55 → #56 → #57 → #58 → #59** (branches `claude/hearth-161-concept-packs-2tf8jk` through `…-2tf8jk-b6`, opened 2026-08-26 02:33–10:27 UTC; #54 targets `main`, each later PR targets its predecessor's head branch — merge in stack order, retargeting as predecessors merge). The stack dispositions the 161 adopted tag-gap concepts as engine packs: concept census **153 → 239** per the PR bodies' own stated numbers (#54: "153 → 168", G3 "all 168 concepts demonstrated"; #59: "Concept census 223 → 239" and "This completes the 161-row review: every row is now dispositioned"); **zero NO-MEASURABLE-EFFECT packs merged** (#54: "Zero NO MEASURABLE EFFECT verdicts"). Each of the six carries a standing-down CI-triage comment: every red leg matches the pre-existing J39/J52 signature classes exactly (e.g. #58's comment tabulates run 32943823904 against its base run — identical J39 canary + J52 fetch/cascade, "no red is introduced by this PR"). Main itself had **not** moved at 10:55 UTC — still `031ed69`. See first-hour step 6, the Deferred-roadmap entry, and `/mnt/project-files/research/bible-rollout/engine-pack-backlog.md` (the durable per-concept ledger + the 50-row corpus-blocked deferral roster riding PR #53). **Third update 2026-08-26 (observed ~22:09 UTC):** main advanced three times in the afternoon — `8c8b78d` (PR #54, merged 14:18 UTC), `6367855` (PR #60, the batches-2–6 recovery: #55–#59 had merged 14:19–14:21 UTC un-retargeted onto the batch branches, so #60 carried their reviewed content to main; merged 14:53 UTC), and **`e762d1c` (PR #53, the J52 re-pin, merged by jestek-dev 15:22 UTC) — now HEAD**. Concept census on main is now **239**; the post-re-pin identity is engine 0.14.0 / corpus `644b241c…` / layer `b24ea16d…`. **Open PRs are now exactly one: draft #61** (the defect-#5 `gauntlet.yml` fix, opened 22:06 UTC — see Known defects #5 and Branch dispositions); #53 and the whole #54–#59 stack are closed.
- **Engine version:** `0.14.0` — `engine/package.json` and `engine/src/config/engineVersion.ts:57` agree. The root `package.json` (`0.1.0`) is just the private monorepo shell.
- **Released version:** npm latest is still **0.7.1** (releases v0.7.0 and v0.7.1, both 2026-07-31, are the only ones). Engine 0.9.0 has been on main since 2026-08-11 (commit `12a0593` — consistent with the J39 approval records pinning 0.9.0 with reviewedAt 2026-08-10/2026-08-17); 0.10.0 through 0.14.0 landed 2026-08-23 via the consolidated PR #43 stack (#35, #36, #37, #39, #40, #42 — #41 merged to main separately per PR #43's own body, "Main is still at fbf2655 (only #33, #34, #41)"; #38 also merged to main directly, but as commit `b3f491d` in main's first-parent history, landing minutes after #43 — #43's body predates it and does not mention it). Nothing newer than 0.7.1 is published, and nothing newer than the schema-5 v0.7.1 pair exists for consumers to pin until the next mint (v0.14.0 — see the Release runbook).
- **Concept census:** **153** files under `ontology/concepts/` (122 after PR #33 → 131 pre-#51 → 153 after #51). Current layerFingerprint `954f04dc…`.
- **The Study:** the search-first dashboard is the default page at `/` (`workbench/static/index.html`); the old console is preserved byte-identical at `/advanced`; `/study` answers a 302 → `/` (see `workbench/src/secondaryPages.ts:19-22`; flipped in PR #50, decision D40/D41).
- **What PR #51 shipped** (merged 2026-08-25): 20 apologetics concept packs/extensions, fixtures-first, all ADMIT; Jesse's D13 master tags (`mormon-evangelism`, `jehovahs-witness-evangelism`, `apologetics` umbrella); his 2026-08-25 scoping correction (4 `mustNotRank` guards on the bare-"apologetics" fixture); 28 active + 14 pending fixtures. Two packs were **deferred as corpus-blocked** (`leviathan-and-behemoth`, `virgin-birth`) and await the re-pin / corpus-expansion PRs. The merge itself was the admission event for its `[editorial]` anchors and pending ids.
- **Stacked-merge nuance worth knowing:** PR #46 merged into its stacked base branch (`claude/hearth-thread-wrhubh-p7`), not main; PR #47 re-landed the identical tree onto main as `388569e` (verified byte-identical). Not a recovery from a bad merge — just how the stack landed.

---

## Why main CI is red, and exactly what turns it green

Main's `Admission gauntlet` workflow has been red since commit `491f23b` (PR #31, 2026-08-20); the last green run was `32329299152` at `2363a3a`. The newest completed run — **`32881874670` on `caf9fe3`, completed 2026-08-25 18:24 UTC, conclusion: failure** (https://github.com/jestek-dev/scripture-search-engine/actions/runs/32881874670) — fails with exactly **four jobs in two error classes**, both fully diagnosed. Nothing else is broken: workbench and sweep test suites pass in the same jobs.

The scheduled weekly `Source reachability` workflow (`sources.yml`) is red for the same drift, so the red Monday alarms will continue until the re-pin — and note the re-pin's green is transient: the alarm honestly re-fires whenever the rolling upstreams next serve new bytes (details at the end of item (b)). The weekly `Release integrity` workflow is green (latest run 2026-08-24, success).

### (a) J39 — stale baseline approval records

**Failing jobs:** `verify (ubuntu-latest)` and `verify (windows-latest)` — the "Unit tests" step. The sole red test is `eval/test/gauntlet-machine-report.test.ts` (it expects the gauntlet verdict ADMIT).

**Root cause:** `eval/baselines/ordering.snapshot.approval.json` and `eval/baselines/probes.approval.json` both pin `engineVersion "0.9.0"` (with 0.9.0-era fingerprints), while the tree is at engine 0.14.0 with candidate baselines carrying layerFingerprint `954f04dc…`. The G2/G8 gates (`eval/src/gates/orderingSnapshot.ts` and the probes approval gate) treat the approval record as the review act — "Writing it IS the approval act" — so a stale record is a failed gate, by design. PR #51's body confirms: "Residual REJECT is solely the pre-existing G2/G8 stale-approval-record class (committed baselines at engine 0.14.0 vs `*.approval.json` records pinning 0.9.0)."

**What Jesse must do** (timing: designate now, sign after the PR #53 re-pin merges — the records bind the exact fingerprints, and the re-pin moves them; see first-hour step 2):

1. **Designate the independent reviewer** (J-item J39, `/mnt/project-files/plans/2026-08-20-implementation-plan.md:902`). Requirements, per `docs/governance/probe-baseline-review.md`: the reviewer did not author the change, is a distinct identity from the repository owner acting alone, and is named by Jesse per review. There is no standing reviewer role. The plan's relayed caution: PR #31's churn ran at the budget limit — this needs real eyes, not a rubber stamp.
2. **The reviewer reviews the packet**, generated read-only by:
   ```sh
   npm run review-packet --workspace eval -- --before <old-baseline.json> --after eval/baselines/probes.json
   ```
   The packet shows every changed probe's before/after top-10 (added/dropped/moved), metric deltas against `eval/budgets.json`, and prints in its footer exactly the digest and identity values the approval must bind. No code path in the repo authors an approval — the reviewer writes the file by hand.
3. **The reviewer authors the two approval records** (`eval/baselines/ordering.snapshot.approval.json`, `eval/baselines/probes.approval.json`) binding the then-current baselines' digests, engine 0.14.0, and the then-current fingerprints (post-re-pin: `644b241c…` / `2b85fa88…`, with `priorProvenance` chaining the merge-base blobs PR #53's body names), with the independence attestation. The v2 chained-approval schema is documented in `docs/governance/probe-baseline-review.md`, § "Approval schema v2 and the v1 grandfather" (~line 89): `reviewerName`/`reviewerContact`, the `independence` attestation, `evidence` (review-record path + sha256), `reviewPacketSha256` (probe-baseline approvals only — the ordering-snapshot approval binds no packet digest), and `priorProvenance` (required non-null except beside an explicit `bootstrap` field). The staged re-pin docs (`docs/web-repin-staged.md` ~line 160, `docs/openbible-repin-staged.md` ~line 251) give only the chaining context — who authors the chained records in the re-pin PRs — not the schema. J40 (plan `:903`) asked whether in-flight approvals land as v2 directly or v1-then-migrate; on today's main the v1 sunset forecloses the second path — a v1 record with `reviewedAt` after 2026-08-20 fails the gauntlet with a named finding, so the reviewer must author these records directly in v2.
4. **Land the records via a reviewed PR that Jesse merges** — his merge remains the final human gate regardless of who signed.

A worked example of exactly what the reviewer must author exists in **PR #41's body** — its section "Ready-made review packet for Jesse", with all sha256/priorProvenance values precomputed. Those values are now stale (baselines moved through #43…#51, and move again with the re-pin), but the format is the template; PR #53's "J39 interplay" section gives the current merge-base blobs.

### (b) J52 — upstream source drift (three stale pins)

**Failing jobs:** `battery (full artifact) — build` fails at the step "Fetch pinned sources (cache miss)"; `battery (full artifact) — cross-leg agreement` is a pure cascade (`Artifact not found for name: battery-report-Linux` — the build leg never produced it).

**Root cause:** all three pinned upstream sources now serve different bytes than their manifests pin (`pipeline/manifests/`). Verbatim from run 32881874670's build-leg log (2026-08-25):

- `openbible-topics` — checksum mismatch: expected `2239700d48…`, got `fee5234e…`
- `openbible-xrefs` — expected `36d1b198…`, got `2006d1af…`
- `web` — content fingerprint `944e3883…` does not match the manifest's `335445ef…`; "The payload itself changed"
- Final error: `sources unfetchable — re-pin required (docs/source-repins.md)`

**The "got" bytes are themselves a moving target.** The 2026-08-21 audit observed *different* drifted bytes for the two OpenBible files (`2647baf7…` topics, `22c26dd6…` xrefs — the values quoted in `docs/openbible-repin-staged.md` and `docs/source-snapshots-errand.md`), so the upstream files drifted again between 2026-08-21 and 2026-08-25. Only the *expected* (pinned) values are stable — which is exactly why the errand's capture-once rule exists. The capture has since happened: PR #53 (below) pins the bytes downloaded once on 2026-08-25 ~18:19 UTC (`b6f55cc7…` web / `fee5234e…` topics / `2006d1af…` xrefs — the xrefs upstream had indeed rolled past even the 08-21-observed `22c26dd6…`).

A checksum here is the identity of the exact snapshot that was admitted and baked into `corpusFingerprint`/`layerFingerprint`; the one prohibition is **never edit the checksum in place** (`docs/source-repins.md`).

**The live vehicle: draft PR #53** (https://github.com/jestek-dev/scripture-search-engine/pull/53, branch `claude/hearth-thread-wrhubh-j52`, head `09e2794`, opened 2026-08-25 19:21 UTC) — a one-PR re-pin of all three sources that composes the two staged runbooks (`docs/web-repin-staged.md` "PR-α" and `docs/openbible-repin-staged.md` "PR C") into a single PR, with the single-PR deviation from source-repins §3's one-PR-per-source rule disclosed in the body ("accept this knowingly or say the word and it splits"). **The staged runbooks remain the background and method record; PR #53 supersedes them as the executable path.** What Jesse must do:

1. **The errand's remaining human steps (J52a/b).** The capture itself is done: all three archives were downloaded once on 2026-08-25 ~18:19 UTC (capture-once, per `docs/source-snapshots-errand.md` — the WEB URL rolls near-daily) and the exact bytes are preserved verbatim on the orphan branch **`claude/source-snapshots-2026-08-staging`** (tip `1d7d2f4`, 6.7 MB, with `CHECKSUMS.txt` and a README carrying the exact `gh release create` / digest-verification commands). Jesse (or any write-credentialed human) must: create the `source-snapshots-2026-08` GitHub Release, upload those assets, verify the digests, and then delete the staging branch. Until then the manifests' `archiveUrl`s 404 and `check:drift` honestly reports them `archive-rotted`. Also still open: **A5b**, the archive search for the *old* pinned WEB bytes (`3458ca34…`) — the two documented commands need a run from an unrestricted machine, and if no copy exists anywhere, accepting the loss of from-scratch reproducibility of the pre-August snapshot is Jesse's explicit call; **nothing in PR #53 signs that off**.
2. **Review PR #53:** confirm the new pins are the intended upstream editions (old → new table in the body), read the committed delta reports (`docs/reviews/2026-08-25-source-repin-delta-reports.md`: WEB identical over the 5,726-verse witness; topics identical over the witnessed rows; xrefs class-(b) vote movement, 173 of 1,835 witnessed edges, itemized — that list is the J52/A5a review item), and its **FINDING**: the `openbible-subset.json` / `passage-terms-subset.json` catch-up regenerations the staged runbooks called for are deliberately **excluded** (measured: the catch-up breaks 8 golden-fixture expectations across 6 fixtures, which per source-repins §5 is a finding for Jesse, never a quiet fixture edit) and land as their own follow-up PR with his ruling. **Merge only after the release assets exist with verified digests.** Note: the re-pin moves `corpusFingerprint` `d2846ff3…` → `644b241c…` and `layerFingerprint` `954f04dc…` → `2b85fa88…` (engine 0.14.0 unchanged) — which is why PR #53 recommends merging before the J39 sign-off, so the approvals are signed once, against the identity that will actually stand (first-hour step 2).
3. **If Jesse prefers the staged two-PR path instead** (WEB first, then OpenBible), close PR #53 and apply `docs/web-repin-staged.md` then `docs/openbible-repin-staged.md` — but note PR #53 already carries the delta measurements and baseline regenerations those runbooks prescribe, and its captured bytes are the same either way.

**What the re-pin fixes — and the alarm's honest steady state.** The re-pin makes the battery fetch step pass durably (the fetcher tries `archiveUrl` after the rolling URL — `pipeline/src/provenance/manifest.ts`, `retrievalUrls`). The weekly `sources.yml` drift alarm goes green on the first post-re-pin run **and then honestly red again whenever upstream next rolls**: `checkSourceDrift.ts` treats any sourceUrl byte mismatch as fatal `DRIFTED` even for a `rollingSourceUrl` source with a healthy archive (only a `contentSha256` repack-tolerance path exists, and the OpenBible manifests deliberately carry none). Each upstream roll is a new J52-class re-pin decision, by design — expect the Monday alarm to re-fire periodically forever; the fetch step, unlike the alarm, stays green via the archive fallback.

**What the re-pin does not fix — the third battery red, in two layers.** With sources fetchable, the battery build job advances to its next gate, "Committed descriptor matches the rebuilt artifact" (`gauntlet.yml`, battery-build job): it rebuilds the artifact at the committed `builtAt` and requires `git diff --exit-code -- artifacts/content-artifact.json` to be clean, failing with "artifact not reproducible from pinned sources — descriptor stale or pipeline drifted; re-mint required (release-housekeeping 0.5-0.7)". Layer one: the committed descriptor is the stale schema-6 / engine-0.7.1 phantom (Known defects #1) while today's tree builds a schema-9 / 0.14.0 artifact — the next mint's descriptor PR clears this ([Release runbook](#release-runbook) steps 2–3). Layer two, **which survives the descriptor PR**: the rebuild step passes `--built-at` but **not** `--release-tag` (`gauntlet.yml:437`), and `pipeline/src/buildArtifact.ts` writes the descriptor's `release: { tag: … }` field only when that flag is passed (`:581`; the CLI reads it only from argv, `:675`) — so the rebuilt descriptor lacks the minted `release.tag`, the `git diff` is non-empty, and the gate fails deterministically even against a fresh, correct descriptor. Closing it needs a small reviewed workflow fix that **does not exist on main yet** — see Known defects #5 for the exact change. **Sequence an executor will observe:** J52 re-pin (PR #53) lands → fetch step green, `sources.yml` green (until the next roll), battery build red at the descriptor gate (cross-leg still cascading); J39 approvals land → both `verify` legs green; mint's descriptor PR merges → battery build **still red at the descriptor gate** (the dropped `release.tag`); the `gauntlet.yml` fix lands → battery build can finally pass the gate, and main can go fully green (subject to the gate's own Node-toolchain caveat in defect #5). The first stage of that sequence is **observed, not inferred**: PR #53's own `Admission gauntlet` run **32889151337** (head `09e2794`, completed 2026-08-25 19:40 UTC) already demonstrated the post-re-pin shape on the PR branch — "Fetch pinned sources (cache miss)" **green** with the new pins, battery build **red** at exactly this "Committed descriptor matches the rebuilt artifact" gate (its error text verbatim), both `verify` legs red on only the single J39 canary test, and cross-leg agreement red at the first artifact download with everything downstream skipped. The mint itself needs only `npm run verify -- --require-admit` green plus fetchable sources (its "Admission gauntlet" and "Fetch pinned sources" gates) — which J39+J52 provide — so the release process is part of turning main green, not gated behind it.

**Update 2026-08-26 — the re-pin has LANDED.** The blocking human step completed: release `source-snapshots-2026-08` was published by jestek-dev at 14:47 UTC and PR #53's updated body records independent digest verification (all three assets match the staging `CHECKSUMS.txt`; `check:drift` resolves the three former `archive-rotted` 404s to "archive serves the pinned bytes" and ends "Every reachable pin matches what upstream serves"). PR #53 merged at 15:22 UTC as squash commit `e762d1c` (main's HEAD) — after first merging main's concept-pack commits into the branch (branch merge `e446347`; only the two generated baselines conflicted, regenerated with the sanctioned flags, approvals untouched), so the final identity is 0.14.0 / `644b241c…` / **`b24ea16d…`** (not the pre-concept-pack `2b85fa88…` quoted above). The sources are now fetchable on main and the first fully green `sources.yml` run is available (until the next upstream roll). What stays red, per the predicted sequence: both `verify` legs (the J39 canary) and the battery build at the "Committed descriptor matches the rebuilt artifact" gate (the stale v0.7.1 phantom now; the `release.tag` rebuild gap after the mint — whose fix now exists as **draft PR #61**, Known defects #5), with cross-leg still cascading. **But the post-merge gauntlet run on main — 32984930946 on `e762d1c`, created 15:23 UTC — had still not started at the 22:09 UTC observation (status `queued`, no jobs, for nearly seven hours)**, so on main the post-re-pin shape remains predicted (and pre-merge-observed via PR-branch run 32889151337), not yet observed — confirm against 32984930946 once it actually runs. A5b remains open.

Companion dependency record: `docs/corpus-payload-dependency.md`. Related low-stakes call: **J53** (keep the all-files WEB contentSha256, default, or narrow to the verse file).

---

## Open decisions reserved for Jesse

### The J1–J70 approval registry

**The registry exists in exactly one place: `/mnt/project-files/plans/2026-08-20-implementation-plan.md`, Appendix A (lines 849–941).** It is *not* in the repo — the repo's `docs/NEEDS-JESSE.md` is an older register (last updated 2026-07-30) and repo docs reference J-numbers without defining them. A reader landing in the repo alone will never find this list; that is why it is reproduced in summary here. Each entry is a decidable question, most with a recommended default; nothing on the list may be decided by an agent.

**Pastoral & theological (J1–J18, plan lines 855–872)** — includes all seven FLAG-FOR-JESSE calls (J1–J7) from the 2026-08-20 search-quality audit (`/mnt/project-files/search-quality-report-2026-08-20.md`, § "FLAG-FOR-JESSE — theological judgment calls"):

- **J1** — "does God forgive me": should 1 John 1:9's assurance lead a penitent (default: yes)?
- **J2** — "I keep falling into the same sin": conviction-first or restoration-first ordering?
- **J3** — "lord's supper": three sub-calls on whether the 1 Cor 11:20 rebuke may outrank the instituting passages.
- **J4** — remembered-phrase queries: must the quoted verse rank #1 (default: yes, top-3 until a tie-break lands)?
- **J5** — "favor of God": verbatim rebuke (Mal 1:9) vs catered answer (default: catered leads).
- **J6** — marriage-query emphasis: three options; recommended is a crisis-comfort path via new phrases.
- **J7** — aging/dying parent: confirm the wrong grade, approve `caring-for-aging-parents`, rule on the Ps 116:15 gate.
- **J8** — 1 Pet 5:7 surviving-entry weight (0.75 vs 0.85 vs defer to the multi-source restore, recommended).
- **J9** — prosperity-corrective editorial routing: approve or decline (guards stand regardless).
- **J10** — "it is well" alias target: `hope-in-god` (recommended) vs `peace-of-god` vs dedicated pack.
- **J11** — doctrinal-terms wave (justification, sanctification, propitiation, trinity locator, incarnation, baptism, doubt, benediction) — approve/amend each.
- **J12** — bare-word + collapse inventory: single-token exception extension; bare `temptation` routing; skim 108 bare-word + 46 collapse rows.
- **J13** — hymn-alias pack: review ~20–30 alias rows line-by-line.
- **J14** — round-3 corpus-backlog re-judgments: confirm standing postures.
- **J15** — ratify the "harmful" hard-fail criterion.
- **J16** — negative-context watchlist seeds (shipped PENDING; 1 Cor 11:20 marked `pending-J3`).
- **J17** — battery judgment ratification + naming who besides Jesse may author non-provisional judgments.
- **J18** — Jer 4:10 ratification timing (option A recommended: ratify riding the ranking-fix PR).

**Engine weights & ordering (J19–J30, lines 876–887):** J19 exact-phrase taper; J20 sole-evidence floor of 6; J21 passage_terms PMI form/constant; J22 same-concept xref full suppression; J23 Rom 3:25 vs Exod 25:17 measurement-then-ruling; J24 complete-match subsumption; J25 equal-score curated tie-break; **J26** TSK off-phrase ×0.5 discount (an engine bump; with J55 it gates TSK Phase B — `engine/src/corpus/repository.ts:490` and `engine/src/createEngine.ts:137` both name "J26/J55"); J27 OpenBible vote buckets; J28 vote-influence scope; J29 LTR philosophy; J30 nDCG gain scale.

**Query policy (J31–J38, lines 891–898):** J31 spelling edit policy and J32 `corrections` contract field (both shipped in 0.12.0/schema v7 per `engine/CHANGELOG.md` and `docs/implementation-plan.md` §5; the adjacent `suggestion` surface shipped at 0.11.0 — ratification state is theirs to confirm); J33 book-misspelling rule + list; J34 space-separated auto-resolve; J35 book did-you-mean suggestion-only; J36 bare-number fallthrough; J37 hymn copyright posture (title-only); J38 Hymnary.org permission email or drop.

**Governance & baselines (J39–J46, lines 902–909):** **J39** reviewer designation (see above — the plan calls it "the plan's FIRST unblocking act"); J40 approval schema v2 migration; J41 A-tier/S-tier bars; J42 each null→value threshold flip in `eval/budgets.json`; **J43** the sweep numbers block (verified on main: every value in `eval/budgets.json`'s `sweep` key is null until Jesse ratifies — the `$comment` says so); J44 battery working agreement; **J45** explanation-faithfulness audit protocol (50 sampled explanations per release, audited by Jesse or a named designee, recorded in `docs/reviews/` — the sampler tooling shipped in PR #44); J46 topic-gap promotion threshold.

**Licensing, sources, release (J47–J62, lines 913–928):** **J47** the release tag push IS the release decision (the registry writes "v0.9.0"; on today's main the tag is `v0.14.0` — Release runbook); **J48** verse_translation_tokens counsel pass before the descriptor PR merges (the ~14 MB ESV/NIV/NLT-derived table ships publicly for the first time); J49 CC BY attribution passthrough into the §5 contract; J50 v0.7.1 known-issue release-notes annotation; **J51** enable immutable releases before the release tag (default: yes); **J52/J53/J54** (see CI section; J54 = fixture-corpus chapter expansion approval); **J55** TSK doctrinal verdict ("re-review required before any import"); **J56** STEPBible verdicts + ratify the covenant-#1 exclusion of TIPNR's AI-generated person descriptions and the TBCWG dataset; J57 STEPBible lemma anchors go/no-go; J58 embedding-assisted-curation standing process; **J59** pericope preaching-themes pilot (20 pericopes, every theme label, provenance presentation); J60 Thompson 1908 chains product direction; **J61/J62** branch dispositions (next section).

**Mega-sweep process (J63–J70, lines 932–939):** J63 AI-generated test queries under covenant #1 (confirm or restrict); J64 grading rubric ratification; J65 pastoral data skim (`felt-need-map.yaml` + adversarial slogans); J66 triage boundary (TRIAGE-RULES.md); J67 human grading commitment (second grader, crisis tiers, Jesse's own est. 22–45-hour first-episode workload); J68 sweep snapshot archives; J69 crisis-query redaction; J70 S-tier residual authority (the phase cannot self-certify past an S gap). J63/J64 are mechanically enforced: sweep scripts refuse to run without `--j63-acknowledged` / `--j64-acknowledged` + `--confirm-network`.

### Decision items living in PR bodies

- **8 deny-list candidates — PR #33's body** (https://github.com/jestek-dev/scripture-search-engine/pull/33, "Your calls" section): kept-with-reservation collapses `lord`→guidance, `job`→remembered-work-as-for-the-lord, `greater`→victory-in-christ, `build`→gathering-together, `gifts`→spiritual-gifts, `starting`→new-creation, plus (per its 2026-08-22 update) bare `blessing` and bare `sovereign`.
- **32-row decision table — PR #41's body** (https://github.com/jestek-dev/scripture-search-engine/pull/41, "Decision table for Jesse"): rows #1–#32; rows 1–5 were flagged as the pre-merge decisions (providence label, corpus-extension scope, Rev 20:11-15, Ps 113:9 weight, Luke 15 weight, …, row 31 inventory-rows plan, row 32 fixture flips). The PR merged; the table's open rows remain open.
- **PR #51's body** (https://github.com/jestek-dev/scripture-search-engine/pull/51) explicitly leaves open: decision-table rows 28–31 (1 Pet 3:15 span, Titus 1:9 weight, Col 4:6 span, Phil 1:16-vs-17 versification), D11/D14, and the mustNotLead-vs-mustNotRank hardening (D12/F-1).
- **Added 2026-08-26: the #54–#59 concept-pack stack + the backlog file's flagged decisions.** Reviewing and merging the six stacked draft PRs **#54 → #55 → #56 → #57 → #58 → #59** in stack order is now on Jesse's list (first-hour step 6): each PR body carries a full per-concept decision table, and each merge IS the engine-admission event for that batch. Two rows are explicitly flagged in the PR bodies for his read (both in #59: `election-and-predestination`, `head-coverings` — §4-neutral by design, adjudicating nothing). Beyond the PRs, `/mnt/project-files/research/bible-rollout/engine-pack-backlog.md` § "Items flagged for Jesse's decision" records the ledger's Jesse-calls (e.g. the `exile-and-captivity` fold-vs-separate-id routing, roster row 45), and its 50-row corpus-blocked roster is the post-PR-#53 work list. **Update 2026-08-26 (afternoon): the stack itself is dispositioned** — #54 and the #60 recovery merged (see State of main and first-hour step 6); the backlog file's flagged decisions remain open, and the 50-row roster is now unblocked by the PR #53 merge.

### Branch dispositions

- **`claude/hearth-thread-vvwdi2` (tip `c3ff4ba`) — DO NOT DELETE; disposition pending J61 (a required decision).** Its history holds **5 in-copyright PDF blobs** (~23.5 MB) under `Books/`: Phillips *A Topical Bible Guide* (2004), Rich *Growing By Heart* (2004), two White Stone 2003 prayer titles, Murdock *Scripture Every Achiever Should Memorize* — publicly served today, since the repo is public. Evidence: `docs/reviews/2026-08-20-branch-inventory.md` §2. J61's options (plan `:927`): keep as-is with exposure documented / Jesse archives privately then the branch family is stripped (full purge needs GitHub support) / confirm the harvest outputs already on main via PR #31 were what was meant / clarify what "sermons" referred to.
- **`claude/hearth-thread-wrhubh-p7` (`5adb1cf`) and `-p8` (`aa953b6`) — safe to delete on the evidence:** both tips' trees are byte-identical to main commit `388569e` (the PR #47 merge); neither commit is an ancestor of main only because of squash merges. Formal deletion approval is J62; these two branches post-date (or were missed by) the branch-inventory census — "wrhubh" appears nowhere in `docs/reviews/2026-08-20-branch-inventory.md` — as do the rest of the wrhubh family and the 1dfiqu, xpo7um, and s3y66s branches: the bare `claude/hearth-thread-wrhubh` (tip `7fa9110` — the stack's bottom branch: PR #34's head, then PR #35's squash-merge target; its tip tree is byte-identical to PR #35's head `0326c6a`, which was `-p2`'s tip at that squash-merge — the branch has since advanced to `6f17ff6`, the PR #36 squash, whose tree differs, with `0326c6a` in its ancestry — and the Phase-2 content `7fa9110` holds reached main via the #43 consolidation), `-p2`..`-p6`, and `-p6b` (tip `6ffa8a2` — PR #42's head branch; its tree is byte-identical to the PR #43 head `e01db27`, so its content is on main via `031e9fc`). None is on an approved deletion list. **49** branch heads exist on origin in total (`git ls-remote --heads origin`, 2026-08-25 19:45 UTC) — that count includes `main`, the draft-PR-#52 branch, and two branches created 2026-08-25 evening for the J52 work: `claude/hearth-thread-wrhubh-j52` (tip `09e2794`, PR #53's head) and `claude/source-snapshots-2026-08-staging` (tip `1d7d2f4`, the orphan branch holding the captured errand bytes — **delete it only after** the `source-snapshots-2026-08` release assets are uploaded and digest-verified, per PR #53's body); i.e. 48 besides `main`.
- **Draft PR #52** (https://github.com/jestek-dev/scripture-search-engine/pull/52, branch `claude/hearth-wrapup-handoff-27it4g`, commit `21f4c7e`) awaits Jesse's review and merge — nothing merges without a human. **Update 2026-08-26:** merged by jestek-dev at 01:22 UTC (squash merge commit `031ed69`); see Known defects #2.
- **Draft PR #53** (https://github.com/jestek-dev/scripture-search-engine/pull/53, branch `claude/hearth-thread-wrhubh-j52`, commit `09e2794`, opened 2026-08-25 19:21 UTC) — the J52 re-pin; blocked on the human release-upload step and awaits Jesse's review and merge (CI section item (b), first-hour step 3). **Update 2026-08-26:** merged by jestek-dev at 15:22 UTC (squash commit `e762d1c`; head at merge `e446347…` after merging main's concept packs). The staging branch **`claude/source-snapshots-2026-08-staging` is now deletable per the PR's own terms** — the `source-snapshots-2026-08` release exists with digest-verified assets (published 14:47 UTC).
- **Draft PRs #54–#59 (added 2026-08-26)** — the six-PR stacked concept-pack train on branches `claude/hearth-161-concept-packs-2tf8jk`, `…-b2` … `…-b6` (heads `c9b3f57` / `2d55955` / `adc1280` / `73278bb` / `c461559` / `25297ab` at 10:55 UTC). Merge in stack order #54 → … → #59, retargeting each as predecessors merge; do not delete the head branches until the PR stacked on each has merged or been retargeted. All six await Jesse's review — nothing merges without a human. (These six branches and their heads post-date the 2026-08-25 19:45 UTC 49-branch census above.) **Update 2026-08-26 (afternoon):** all six merged 14:18–14:21 UTC, but only #54 landed on main (`8c8b78d`); #55–#59 merged **un-retargeted**, their squash commits landing on the previous batch's branches (per PR #60's ledger: `d7ad39c` / `ab5ec14` / `1d6e8e6` / `421f069` / `2d6835b`). Recovery PR **#60** (base main @ `8c8b78d`, head `…-b6` @ `2e9256b`, carrying exactly the reviewed content of batches 2–6) was merged by jestek-dev at 14:53 UTC as `6367855`. Per #60's body the pack branches (`…-2tf8jk` through `…-b6`) are now deletable; the formal deletion approval remains a J62-class call.
- **Draft PR #61 (added 2026-08-26)** — https://github.com/jestek-dev/scripture-search-engine/pull/61, branch `claude/hearth-wrapup-handoff-27it4g` (restarted from `e762d1c`), commit `39b562a`, opened 22:06 UTC: the Known-defects-#5 `gauntlet.yml` `--release-tag` fix. The only open PR as of 22:09 UTC; awaits Jesse's review and merge — nothing merges without a human. Details in Known defects #5.

---

## Known defects and their state

1. **v0.7.1 committed descriptor is a phantom (known, documented, superseded by process).** The committed `artifacts/content-artifact.json` on main (schema 6, engine 0.7.1, sha `35b7a6f3…`, 137,412,608 bytes) matches **no published asset**; the actually published v0.7.1 `content.db` is schema 5, sha `b57d3676…`, 123,310,080 bytes, 8 concepts — the byte gap is exactly the 14,102,528-byte `verse_translation_tokens` table. The published pair (asset + its release-served descriptor) is internally sound. The descriptor is marked `stale: { since: 2026-08-08, blocksRelease: true }`, which deliberately blocks any release until a fresh mint. **Mechanism defect, precisely:** the v0.7.1-era `release.yml` rebuilt the artifact at tag time and compared the rebuild to the committed descriptor — a CI↔CI toolchain race (setup-node pins only the Node major, so the bundled `node:sqlite` floats), so its green depended on toolchain luck and it published rebuilt, unreviewed-identity bytes. **Fixed:** the current promote-only `release.yml` (PRs #26/#35) rebuilds nothing at tag time — see the [Release runbook](#release-runbook). Standing decision: never republish v0.7.1; ship forward (`docs/reviews/2026-08-21-ship-forward-v090.md` — note its "v0.9.0" designation is stale: the tree has since re-versioned to 0.14.0, and the changelog guard makes the next tag `v0.14.0`; see the Release runbook). Pre-immutability releases are watched at rest by the weekly `release-integrity.yml` (green as of 2026-08-24). Open related items: J50 (annotate v0.7.1's notes), J51 (immutable releases).
2. **`compileJudgments` crash — FIXED AND MERGED: PR #52 merged 2026-08-26 ~01:22 UTC by jestek-dev, squash merge commit `031ed69` (main's new HEAD).** Pre-existing (flagged in PR #49's "Follow-up noticed" section): `workbench/src/compileJudgments.ts:759` iterated `selection.chapters` unconditionally, but P5.2/QR-3 added two verse-level entries to `pipeline/fixtures/web-subset.json` (Song of Solomon `["2:1"]`, 3 John `["1:4"]`) that carry `verses` and no `chapters` key — so any `npm run compile-judgments` against the real repo root crashed with `TypeError: selection.chapters is not iterable`. A stale local `WebSubsetFile` interface (declaring `chapters` required, contra the authoritative `Selection` in `pipeline/scripts/generateFixture.ts`) hid it from TypeScript. The fix iterates `selection.chapters ?? []`, corrects the interface, and deliberately grants verse-level entries **no** chapter membership (counting the chapter as sampled would make fixtures vacuously "covered"). Regression-tested; no engine code, no ordering change, no `ENGINE_VERSION` bump.
3. **Windows-only environmental flake** — an `EPERM` rename (errno `-4048`) in `workbench/test/blindComparison.test.ts`'s deliberate lock-contention race (the `applyJournal.ts` lock-reclaim path), noted in **PR #41's body**; a re-run cleared it. (A remembered "telemetry-test flake on PR #50" could not be verified — PR #50's Windows logs show `telemetryAudit.test.ts` 6/6 passing; the PR #41 note above is the nearest real fact.)
4. **`jobRunner.test.ts` / `server.integration.test.ts` fail in the sandboxed dev container only** — they pass in GitHub CI (noted in PR #50's body and reconfirmed on clean main during the PR #52 work). Environmental, not a product defect.
5. **The battery descriptor gate cannot go green as `gauntlet.yml` stands — the rebuild drops `release.tag` (workflow fix required, not yet written).** The battery-build step "Build release artifact at the committed builtAt" (`gauntlet.yml:427-437`) invokes `build:artifact` with `--out` and `--built-at` only. `pipeline/src/buildArtifact.ts` emits the descriptor's `release: { tag: … }` field **only** when `--release-tag` is passed (`:581`: `...(releaseTag === undefined ? {} : { release: { tag: releaseTag } })`; the CLI reads the flag from argv at `:675`), and the build always rewrites `artifacts/content-artifact.json` in the worktree. The minted descriptor the descriptor PR commits **does** carry `release.tag` (the mint passes `--release-tag`; `release.yml` requires the field). So after the descriptor PR merges, the rebuild regenerates the descriptor *without* `release`, `git diff --exit-code -- artifacts/content-artifact.json` is non-empty, and "Committed descriptor matches the rebuilt artifact" fails on every run — deterministically, even though the descriptor is fresh and correct. **The fix** (a normal reviewed PR Jesse must merge; it does not exist on main as of 2026-08-25 19:45 UTC): feed the committed descriptor's own tag back into the rebuild — e.g. in that step, alongside the existing `BUILT_AT` extraction, read `RELEASE_TAG=$(node -p "JSON.parse(require('fs').readFileSync('artifacts/content-artifact.json','utf8')).release?.tag ?? ''")` and append `--release-tag "$RELEASE_TAG"` when non-empty (reading the committed field keeps the gate self-contained: the rebuild reproduces exactly what the descriptor claims, with no new inputs). A natural vehicle is the battery **activation PR** that `gauntlet.yml`'s own two-phase-activation comment already anticipates (first checker-green run pasted in, then Jesse flips the job to a required check). One honest caveat even after the fix: this gate's green also depends on the battery runner's `node:sqlite` reproducing the mint run's bytes at the same `builtAt` — both workflows pin only the Node major (`node-version: '24'`), i.e. the same toolchain-luck mechanism defect #1 describes as fixed on the *release* path still underlies the *battery* path's reproduction check. **Update 2026-08-26: the fix now exists as draft PR #61** (https://github.com/jestek-dev/scripture-search-engine/pull/61, opened 22:06 UTC by claude[bot]; branch `claude/hearth-wrapup-handoff-27it4g` — PR #52's old head branch name, restarted from `e762d1c` — single commit `39b562a`, base main @ `e762d1c`). Diff verified against this entry: one file, `gauntlet.yml` — the battery rebuild step reads `RELEASE_TAG` from the committed descriptor exactly as prescribed above and appends `--release-tag "$RELEASE_TAG"` **only when non-empty**, so it is a deliberate no-op until a mint lands a descriptor carrying `release.tag`; a comment block in the step explains the mechanism. Awaiting Jesse's review and merge; as of 22:09 UTC the PR carries **no GitHub review or comment** (the go-ahead to open it as a draft was conversational, not an API-visible approval). The toolchain-luck caveat above still stands after this fix.
6. **The v0.14.0 full artifact exceeds the G10 size budget — the release runbook will strand at the descriptor PR (step 3) until a reviewed `eval/budgets.json` decision lands. Found 2026-08-26.** A fresh release-shaped build from main @ `e762d1c` (239 concepts, schema v9) measures **167.84 MiB** against G10's `totalArtifactBytes` budget of **167,772,160 bytes = 160 MiB** (`eval/budgets.json` → `size`) — **~7.8 MiB (~4.9%) over** — and two per-table sub-budgets are also over: **`spelling_deletes` 15.7 MiB vs its 12 MiB budget** and **`spelling_terms` 1.1 MiB vs its 1 MiB budget** (indexes included, per the budget's own convention). Evidence, twice over: the 2026-08-26 battery re-grade (`/mnt/project-files/research/battery-regrade-2026-08-26.md`, §6 "NEW OBSERVATION", full identity in its §1: sha256 `fb570762…`), and independently PR #61's battery-build CI log, whose rebuilt `content.db` is the same 167.84 MiB. **Why no CI is red for this today:** G10 (`sizeGate` in `eval/src/gauntlet.ts`, ~lines 307–393) checks (a) the artifact the gauntlet run itself built — in per-PR CI that is the small *fixture* artifact — and (b) every descriptor under `artifacts/` — currently the stale v0.7.1 phantom at 131.05 MiB, whose schema-6 `perTableBytes` has no spelling tables at all. Both are under budget, so the required `verify` legs pass G10 on main. The battery job's gauntlet *does* run G10 over the real bytes and it fails there — but that job's non-G12 gates are tolerated by design (`continue-on-error`; the G12 checker is the sole success criterion), so the failure is advisory-visible, never blocking. **Exactly where the mint flow stops:** the mint workflow itself **completes** — `mint-artifact.yml`'s "Admission gauntlet" step (`npm run verify -- --require-admit`) runs *before* "Fetch pinned sources"/"Build the canonical artifact", so it sees only the fixture artifact plus the old committed descriptor, and nothing re-runs the gauntlet after the build (`buildArtifact.ts` reads `budgets.json` only for G5 distinctiveness; it enforces no size budget). The 167.84 MiB bytes land on the draft release and the minted descriptor is emitted. The hard stop is **runbook step 3, the descriptor PR**: the minted descriptor carries `databaseBytes` ≈ 167.84 MiB and the oversized `perTableBytes`, so the required `verify` legs' G10-size gate REJECTs with three findings — verbatim shapes: "artifact is 167.8 MiB, over the 160 MiB budget. Tighten a pruning threshold in eval/budgets.json or reduce admitted rows."; "spelling_deletes is 15.7 MiB, over its 12 MiB budget (indexes included)…"; "spelling_terms is 1.1 MiB, over its 1 MiB budget (indexes included)…" — `--require-admit` exits non-zero (and the Unit-tests gauntlet-verdict canary goes red with it), so the descriptor PR cannot merge. Release step 5's own `verify -- --require-admit` would refuse the tag for the same reason even if the PR were forced. **What grew (measured, not mysterious):** vs the committed 131.05 MiB schema-6 descriptor, the delta of +36.8 MiB is essentially two new reviewed capability tables — ~+16.8 MiB of schema-v7 spelling tables (the ED-2 delete-neighborhood table; `budgets.json`'s `$comment_spelling` explicitly deferred the full-corpus check "at the next mint" and pre-commits a **reviewed mitigation ladder** for exactly this event: (1) df ≥ 2 floor on corpus-origin vocabulary, (2) ED-2 depth only at term length ≥ 10, (3) ED 1 everywhere) and ~+21 MiB of schema-v9 `cross_reference_phrases` (TSK phrase capability, measured-simulated ~21.2 MiB, budgeted 24 MiB) — with the 33 → 239 concept growth minor by bytes. **The structural fact for the review:** the per-table budgets in `size.perTableBytes` now sum to **~245 MiB** while `totalArtifactBytes` is 160 MiB — the file's own reviewed sub-budgets no longer fit inside its own total, because the total dates to 2026-07-29 (`f33947b`), sized against the 117.60 MiB first measured artifact ("~122 MiB corpus + ~30 MiB layers"), and was never re-derived when the spelling (schema v7) and xref-phrase (schema v9) budgets were reviewed in via `031e9fc`. **Two fix paths — Jesse's call either way:** **(a) size reduction** — the only meaningful non-corpus mass to cut is `spelling_deletes` via its pre-reviewed ladder (step 1 alone may or may not clear 12 MiB; steps 2–3 shrink hard but degrade the misspelling category the re-grade just measured going F → B, with cited corrections on all six ms queries) and/or pruning/dropping capability-only `cross_reference_phrases` (~21 MiB, but it is TSK Phase B's reviewed data landed deliberately in #42) — i.e. ~8 MiB of cuts must come out of shipped, reviewed value; **(b) a reviewed `budgets.json` bump** — a normal PR Jesse reviews raising `totalArtifactBytes` to a number he sets from the measured 167.84 MiB plus headroom (e.g. 176 or 192 MiB), and settling the two spelling sub-budgets (ladder step 1 first, per the pre-reviewed commitment, or a measured bump quoting the observed sizes). **Recommendation: (b), with the spelling ladder's step 1 evaluated alongside it.** The overrun is the arithmetic consequence of deliberate, measured, individually-reviewed additions — the sub-budgets already sum to ~245 MiB — so the stale number is the 160 MiB total, not the artifact; raising it citing the measured build is precisely the reviewed-data change CLAUDE.md's gate discipline prescribes ("Thresholds live in `eval/budgets.json` as reviewed data"), whereas cutting ~8 MiB means un-shipping value the same review process admitted. Either way the budgets PR should land **before or alongside** the descriptor PR (a mint run is not wasted — the draft's bytes keep; the descriptor PR simply cannot merge first).

### Known behavior (not a defect), 2026-08-26: the 14 `pastoral-*` packs' engine ids are unprefixed — display tags ≠ engine concept ids

Verified against `origin/main` @ `e762d1c`. The 14 `ontology/concepts/pastoral-*.yaml` files all carry an `id:` field **without** the `pastoral-` prefix, and for 12 of the 14 the id is exactly the filename minus prefix; two diverge further: `pastoral-refuge-and-justice.yaml` → `id: refuge-and-justice-for-the-oppressed` and `pastoral-serious-illness.yaml` → `id: serious-illness-and-dying`. Full mapping (filename stem → engine id): betrayal-and-marriage-crisis, freedom-from-bondage, god-sees-my-suffering, grief-and-loss, hope-in-despair, marriage-divorce-teaching, near-to-the-brokenhearted, prayer-for-healing, pregnancy-and-child-loss, relapse-and-restoration, sexual-purity, strength-in-weakness each map identically (strip `pastoral-`), plus the two renames above. `related:` edges across the ontology already use the unprefixed forms (e.g. `pastoral-grief-and-loss.yaml` relates to `pregnancy-and-child-loss`, `near-to-the-brokenhearted`), so the engine layer is internally consistent.

**The artifact carries the unprefixed ids — verified empirically, not just from code.** Code path: `pipeline/src/importers/ontologyImporter.ts` keys every row on the parsed `id:` field (`:111-120`, `concepts.push({ id: parsed.id, label: parsed.label })`; the filename is used only for deterministic sort order and error messages), and `pipeline/src/buildConceptLayer.ts:176-177` inserts `concept.id` verbatim into the `concepts` table. Confirmed by rebuilding the fixture db (`pipeline` → `npm run build:fixture`) from a worktree byte-identical to main @ `e762d1c` for `ontology/` + `pipeline/` + `eval/` — final layer fingerprint `b24ea16d…`, matching the post-#53 identity recorded above — and querying it: **zero** concept ids contain "pastoral"; all 14 unprefixed ids present; 239 concepts total.

**The display layer uses the PREFIXED form by rule.** `research/bible-rollout/CONVENTIONS.md` §5 mandates exact ids and explicitly says "never strip a prefix (`pastoral-god-sees-my-suffering`, not `god-sees-my-suffering`)", and the book docs follow it (spot-checked: `pastoral-grief-and-loss` in `1-samuel.md:245`, `pastoral-serious-illness` / `pastoral-strength-in-weakness` in `2-corinthians.md:67,119`).

**Practical consequence:** a display tag string is **not** always the engine concept id. All 14 prefixed `pastoral-*` tags used in the book docs resolve to no row in the artifact's `concepts` table (16 divergent strings counting the two renamed ids). Anyone wiring display tags to engine queries later needs a prefix-strip plus the two-rename mapping — an exact-string lookup will silently miss. This is a naming split between two internally-consistent layers, not a ranking or correctness bug; nothing is broken today because tags are display-only (§5's own framing).

**If alignment is ever wanted**, it is a reviewed engine-data PR someone can propose later (renaming concept ids touches `related:` edges, fixtures, and moves the layer fingerprint — ordering-relevant, so full gauntlet + review). Deliberately **not** queued for Jesse now.

---

## Deliverables map — `/mnt/project-files/`

> **[Superseded 2026-08-27 →]** No longer true that losing the share loses the research: every file under `/mnt/project-files/` was copied verbatim into the repo at `project-handoff/project-files/` on 2026-08-27 (the 61 screenshot PNGs, initially excluded as binaries, were copied in at the same day's verification pass — see `project-handoff/INDEX.md` for the complete accounting). The J1–J70 registry now lives in-repo at `project-handoff/project-files/plans/2026-08-20-implementation-plan.md`. The map below remains accurate as a guide to what each file is, and 2026-08-26/27 added: `plans/2026-08-26-whole-bible-coverage-plan.md`, `plans/2026-08-27-sweep-adjudication-plan.md`, `plans/2026-08-27-votes-to-engine-plan.md`, `rulings/2026-08-27-theology-rulings-ledger.md`, `research/battery-regrade-2026-08-26.{md,json}`, `research/bible-rollout/sweep/` (66 ledgers), `research/bible-rollout/corpus-expansion-ruling-packet.md` + `-supplement.md`, and `research/popular-verses/held-fixture-drafts/`.

These files exist **only** on the project-files share, not in the repo (except where noted). Losing this directory loses the J-registry and all curation research.

### `plans/`

- `2026-08-20-implementation-plan.md` — **the 9-phase master plan carrying the J1–J70 Appendix A registry (lines 849–941). The single most important file here; it exists nowhere else.**
- `2026-08-21-workbench-design-prompt.md` — the design brief that produced The Study.
- `2026-08-22-dashboard-implementation-plan.md` — The Study's implementation plan (the repo carries a copy at `docs/plans/2026-08-22-dashboard-implementation-plan.md`).

### `research/`

- `bible-rollout/` — **65** per-book tagging docs (note: `genesis.md` does not exist here — Genesis was the pilot; its material is `research/2026-08-22-genesis-pilot-summaries.md`), plus `CONVENTIONS.md` (whose **§11 addendum, binding 2026-08-25, adopts the 161-concept display-tag vocabulary and tag rulings**), `tag-gaps.md` (2,797 lines) and `tag-gaps-review.md` (2,908 lines) — the gap analysis and its review that produced the adopted vocabulary. **Added 2026-08-26: `engine-pack-backlog.md`** — the 161-concept rollout's durable record: the per-PR batch/census/fingerprint table for the #54–#59 stack, the per-batch summary, the **50-row corpus-blocked deferral roster** (the post-PR-#53 re-pin work list), and § "Items flagged for Jesse's decision". **Also added 2026-08-26: `tag-apply/adopted-concepts.md`** — the canonical CONVENTIONS §11.1 adopted-concepts list (161 ids, each marked engine-built yes/no against the 239-concept census; reconstructed 2026-08-26 from `tag-gaps-review.md` — closes the Known loose end below, see its dated update).
- `apologetics-tags/` — `apologetics-concept-map.md` (1,419 lines; the sole anchor source PR #51 names) + `apologetics-concept-map.json`.
- `popular-verses/` — `top-200-verses.json`, `top-200-verses.md`, `dedupe-log.md`, `build_ranking.py`, and `sources/` (biblegateway, listicles, memorization, search-volume, youversion JSON) — the popularity dataset and its full provenance.
- `2026-08-22-genesis-pilot-summaries.md` — the Genesis tagging pilot.
- Assessments: `2026-08-21-uploaded-docs-assessment.md`, `2026-08-22-chapter-summaries-assessment.md`, `2026-08-22-keller-tagging-and-chapter-tags-assessment.md`.
- Screenshot evidence: `2026-08-21-prototype-audit/` (16 PNGs), `2026-08-22-prototype-v2/`, `2026-08-22-study-p1/` … `2026-08-23-study-p5/` (The Study, phase by phase).

### Top level

- `guardrail-audit-2026-08-15.md`, `theological-guardrail-recommendation-2026-08-15.md` — the theological guardrail audit and its recommendation.
- `new-books-assessment-2026-08-15.md` — assessment of the candidate books (feeds J55/J56/J60/J61 territory).
- `search-quality-report-2026-08-20.md` — the measured search-quality baseline that drove the 0.10.0+ fix train.
- `book-reference-tables/` — `growing-by-heart.json`, `phillips-topics.json`, `scriptural-prayers.json` (harvested reference tables from the J61 books).
- `uploads/hearth/` — 5 raw uploaded files (uuid-named).

**Known loose end:** `CONVENTIONS.md` §11 refers to the adopted-concepts list as "the working inventory's `tag-apply/adopted-concepts.md`", but **no such file exists anywhere under `/mnt/project-files`** — the 161-id list's file location is unresolved. The count and the adoption ruling are in §11 itself; if the standalone list matters, it needs to be regenerated from `tag-gaps-review.md`. **Update 2026-08-26: RESOLVED — the file now exists**, at `/mnt/project-files/research/bible-rollout/tag-apply/adopted-concepts.md`, reconstructed 2026-08-26 from `tag-gaps-review.md` exactly as this record prescribed. Verified 2026-08-26: exactly **161** concept-id lines (`tag-gaps-review.md` §2's 162 proposed entries — theme counts A–M sum to 162, confirmed — minus the review's one recorded fold, `waiting-and-timing-in-love` → `romantic-love-and-intimacy`, which the file's fold note records; the folded id is absent and the fold target present); spot-checked ids (`blasphemy-against-the-spirit`, `kinsman-redeemer`, `zion-city-of-god`, `i-am-sayings`) all trace to §2 headings. Per the file's own header (not independently re-derived): each id carries an engine-built yes/no marker against the 239-concept census at `origin/main` as of 2026-08-26, with full provenance (the §11.1 citation, Jesse's 2026-08-25 adoption ruling, and the first-named-id rule for "or …" alternatives) in the header.

---

## Deferred roadmap

- **Engine packs for the 161 adopted display-tag concepts.** `CONVENTIONS.md` §11 is explicit: the adopted ids are approved vocabulary-addition *candidates*, display-only — "Nothing here creates a concept pack; any engine admission still requires golden fixtures, a gauntlet run showing measurable effect, and a human PR merge… `NO MEASURABLE EFFECT` still means don't merge." Ingestion goes through the normal fixtures-first gauntlet flow and needs Jesse's go, concept by concept. **Update 2026-08-26 ~10:55 UTC — no longer deferred: dispositioned.** All 161 rows were worked through the fixtures-first gauntlet flow on 2026-08-26 and now sit as the six stacked **draft** PRs **#54 → #55 → #56 → #57 → #58 → #59** awaiting Jesse's review and merge in stack order (census 153 → 239 per the PR bodies; zero NO-MEASURABLE-EFFECT packs merged; each merge is the admission event for its batch — nothing is admitted until he merges). **Further update 2026-08-26 (afternoon): merged** — #54 to main directly (`8c8b78d`), batches 2–6 via recovery PR #60 (`6367855`; #55–#59 had merged un-retargeted — see State of main); census is 239 on main. The corpus-blocked remainder — a **50-row deferral roster** (49 review rows + the standing `virgin-birth` item) that rides the PR #53 re-pin — plus the items flagged for Jesse's decision live in `/mnt/project-files/research/bible-rollout/engine-pack-backlog.md`, the rollout's durable per-concept ledger (also carries the per-PR batch/fingerprint table).
- **Pericope-level anchors + preaching-themes pack** (the item sometimes remembered as a "section-level tagging sweep" — no item by that name exists in any plan; this is the match): plan item **P6.6 · B7** at `/mnt/project-files/plans/2026-08-20-implementation-plan.md:630-638` — a 20-pericope pilot on the sections data that already landed (P5.6, schema v8 — PR #40, landing on main via the #43 consolidation, commit `031e9fc`). Gated on **J59**.
- **Mega-sweep certification, MS-12..14** (plan `:820-845`): the fix loop, sweep-to-sweep replay comparison (promoted-fixture cap ≤250), and the exit-bar certification (`docs/reviews/sweep/CERTIFICATION-<date>.md` — "the phase ends with his signature or it does not end"). Deliberately **not implemented** (PR #46's body records the directive). Unblock order: ① PR #44 merged (done) → ② J39/J52 → ③ the terminus mint (P7.6) → ④ J43 signing + J63–J70 rulings → ⑤ implement MS-12..14 and run a certified sweep. `eval/budgets.json`'s `sweep` block is all-null awaiting J43.
- **TSK Phase B** (cross-reference scoring; data capability landed via #42) — gated on J26 + J55. **STEPBible** — gated on J56/J57. **Thompson chains** — gated on J60.

---

## Consumers and pinning

Three apps — **Maskil, LH Worship Setlist, Versed** — consume the engine. The canonical contract is `docs/implementation-plan.md` **§5 "Runtime API (consumer contract)"** (line 285, in the repo); `docs/CONSUMERS.md` (landed via PR #44) carries the pin discipline, with §5 canonical where they overlap:

- Consumers pin **both** halves: the exact engine semver (`@jestek-dev/scripture-engine@X.Y.Z`) **and** the artifact descriptor (`content-artifact.json` committed into the consumer's own repo, naming release tag + `databaseSha256` + the three-fingerprint identity `engineVersion`/`corpusFingerprint`/`layerFingerprint`).
- "Upgrading either half is an explicit re-pin of both": check `docs/COMPATIBILITY.md`, read `engine/CHANGELOG.md` forward, re-verify the artifact.
- **A new release therefore changes nothing for any consumer until that consumer deliberately re-pins.** The only published pair to pin is the schema-5 v0.7.1 pair (npm serves nothing newer); the consumers' actual current pins live in each consumer repo's own committed `content-artifact.json` and could not be verified from here — check each before assuming it. The API surface has grown additively since (`suggestion` at 0.11.0, `corrections` at 0.12.0/schema v7, more at 0.14.0/schema v8), so the next re-pin — against the **v0.14.0** release the runbook cuts — is where they pick those up. Note that main's pipeline now builds **schema v9** (`pipeline/src/schema.ts`): v8 = pericopes; v9 adds the TSK-derived `cross_reference_phrases` table, capability-only — no engine code path reads it yet, so a v9 artifact ranks byte-identically (PR #42; `engine/CHANGELOG.md` 0.14.0 entry). The v0.14.0 mint will therefore emit a schema-**9** artifact — expected, not a defect — and consumers coming from the schema-5 v0.7.1 pair should expect that jump (schema 5 → 9, engine 0.7.1 → 0.14.0, in one re-pin). CC BY attribution passthrough (OpenBible) rides the release notes and §5 — pending J49.
- Per `CLAUDE.md`: check §5's consumer contract before changing any public type — changes here ripple to three apps.

---

## Release runbook

**Currently blocked.** `release.yml`'s first gate refuses a `stale.blocksRelease: true` descriptor (which the committed one has), and the mint requires the gauntlet green with `--require-admit` — so nothing can be cut until **J39 and J52 land**. The release-verification rehearsal (`docs/release-verification-rehearsal.md`) itself "queues behind J39". Once the `verify` legs are green and the pinned sources are fetchable, Jesse can execute this alone — note that main's battery legs stay red throughout this runbook and beyond (the stale-descriptor gate, then the `release.tag` workflow gap — Known defects #5), so this runbook is part of turning main green, not something to wait on main for.

**The tag is `v0.14.0` — not the "v0.9.0" the older release docs name.** The changelog guard (`.github/scripts/changelog-guard.mjs`, `checkChangelog`, run unconditionally by `release.yml`'s "Refuse to release an unchangelogged version" step before anything publishes) fails any pushed `v*` tag whose version does not equal `engine/package.json`'s version — verbatim: "Pushed tag v0.9.0 does not match engine/package.json version 0.14.0 — the publish would ship a version the tag does not name." Engine is 0.14.0 on main, with a `## 0.14.0` CHANGELOG entry present (the P7.1 backfill), so a `v0.14.0` tag passes and a `v0.9.0` tag fails deterministically — *after* the mint and descriptor PR are done, leaving a dead tag and a descriptor naming a tag that can never promote. The "v0.9.0" designation in `docs/reviews/2026-08-21-ship-forward-v090.md` and `docs/descriptor-pr-template.md` was written 2026-08-21, when main's engine WAS 0.9.0; the 0.10.0–0.14.0 train (merged 2026-08-23 via #43) obsoleted the number, and npm will publish whatever `engine/package.json` says. Two honest paths: **(1) recommended — mint and tag `v0.14.0` on the tree as it stands** (nothing but the number changes; the ship-forward decision's substance — ship forward, never republish 0.7.1 — carries over intact); **(2) deliberately re-version the tree first** to some other number — that is its own J-class call, needs a version + `ENGINE_VERSION` + CHANGELOG change in one commit *before* the mint, and nothing in the current docs argues for it. J47/J48/J50/J51 attach to the tag whatever it is named. **Stale-template warning:** `docs/descriptor-pr-template.md` §2.1's "structurally fixed" checklist values — `engineVersion: "0.9.0"`, `schemaVersion: "6"`, `release: { tag: "v0.9.0" }` — are equally stale; today's mint emits `0.14.0` / `"9"` / the tag you actually request. Read them as *the-values-must-be-checked*, not as the values themselves; fixing the template is a small to-do that can ride the descriptor PR.

The machinery (PRs #26/#35) is **promote-only**: the canonical bytes are built exactly once, attested, and parked on a draft release; the tag push verifies and promotes — **nothing is rebuilt at tag time** (this is what closes the v0.7.1-class hole).

1. **Preconditions:** `npm run verify -- --require-admit` green on main and pinned sources fetchable — the mint workflow's own gates ("Admission gauntlet" then "Fetch pinned sources", its second and third steps; the literal first is a trivial "Validate the requested tag" format check); the stale descriptor is about to be replaced by the mint, and the battery legs' red at the descriptor gate is expected here, not a blocker. Decide J51 (immutable releases — default yes, do it before the tag) and clear J48 (verse_translation_tokens counsel) before step 3's PR merges.
2. **Human — dispatch the mint:** run the `mint-artifact.yml` workflow (workflow_dispatch, input `release_tag`), via the Actions UI or `gh workflow run mint-artifact.yml -f release_tag=v0.14.0`. **CI then:** validates the tag format → verifies `--require-admit` → fetches pinned sources → builds the artifact fresh (`build:artifact --release-tag …`; the bytes are canonical, never reproduced) → **attests** provenance (actions/attest-build-provenance) → creates/reuses a **draft** release at the tag and uploads `content.db` → emits the minted descriptor as a workflow artifact plus a committed-vs-minted fingerprint diff table.
3. **Human — the descriptor PR:** commit the minted `content-artifact.json` (now carrying `release.tag`) via a reviewed PR using `docs/descriptor-pr-template.md` (with the §2.1 staleness correction above). Merging this PR is the release decision's review. **It does not turn the battery jobs green** — the rebuild-side `release.tag` gap (Known defects #5) keeps the descriptor gate red until the separate `gauntlet.yml` fix lands. **Caution added 2026-08-26 — this PR will REJECT on G10 as budgets stand (Known defects #6):** the v0.14.0 build measures 167.84 MiB vs the 160 MiB `totalArtifactBytes` budget (with `spelling_deletes` 15.7 vs 12 MiB and `spelling_terms` 1.1 vs 1 MiB also over), and committing the minted descriptor is the first place a *required* check reads those numbers — the `verify` legs' G10-size gate fails, `--require-admit` exits non-zero, and this PR cannot merge until Jesse lands a reviewed `eval/budgets.json` decision (bump or prune — his call; recommendation and full mechanics in defect #6). The mint (step 2) completes regardless and its draft is not wasted; resolve the budget question before or alongside this PR.
4. **Human — push the tag** (`git tag v0.14.0 && git push origin v0.14.0`). Per J47, the tag push IS the release decision — npm publication is effectively irreversible.
5. **CI (`release.yml` on the tag):** refuses a stale descriptor → changelog guard (`.github/scripts/changelog-guard.mjs` — `engine/CHANGELOG.md` must have an entry for `engine/package.json`'s version, **and** the pushed tag must equal that version, **and** the tag's own version must have an entry; any miss is a refusal) → `npm run verify -- --require-admit` → asserts `descriptor.release.tag === tag` → downloads the DRAFT's `content.db` → **verifies its sha256 and byte count against the committed descriptor** → `gh attestation verify --repo … --signer-workflow …/mint-artifact.yml` → `npm pack` with the dist-guard (tarball must contain `package/dist/index.js` — the v0.7.0 empty-package defect) → npm publish via OIDC trusted publishing (idempotent if the version exists) → uploads descriptor + tarball and flips `--draft=false` (assets frozen if immutable releases are enabled) → post-release smoke job. A drifted asset fails the tag; nothing unreviewed can ship.
6. **Verification (anyone, including consumers):** download the release's `content.db`, hash it, compare to the committed descriptor's `databaseSha256`/`databaseBytes`; the engine's admission check also verifies identity at open (`docs/CONSUMERS.md` quickstart). Descriptor ↔ artifact can no longer diverge silently: the tag path proves the equality before promoting, and `release-integrity.yml` re-checks published digests weekly.
- Steady-state afterwards: data-only releases go out as `artifact/<date>` tags with no engine bump (`docs/plans/2026-08-14-implementation-plan.md` §7.4).
- **After the release:** land the `gauntlet.yml` `--release-tag` fix (Known defects #5, natural vehicle: the battery activation PR) — only then can the battery jobs, and therefore main, go fully green.

---

## Verification notes

Everything above traces to the repo at `origin/main` `caf9fe3`, GitHub API state on 2026-08-25, the CI logs of run 32881874670 (and its predecessors), the PR bodies cited, and the files under `/mnt/project-files` — all checked on 2026-08-25. The J52 "got" checksums are quoted from run 32881874670's build-leg log itself (job "battery (full artifact) — build", step "Fetch pinned sources"); the older `2647baf7…`/`22c26dd6…` values that appear in the staged repin docs are the 2026-08-21 observations, retained above only to show the drift-between-observations point. Three things could **not** be verified and are flagged inline: the standalone `tag-apply/adopted-concepts.md` file (referenced by CONVENTIONS.md §11 but not found anywhere — **resolved 2026-08-26**: the file was reconstructed from `tag-gaps-review.md` and now exists at `research/bible-rollout/tag-apply/adopted-concepts.md` with a verified 161-id count; see the Deliverables-map Known-loose-end update), the remembered "telemetry-test Windows flake on PR #50" (the verifiable fact is the `blindComparison.test.ts` EPERM flake noted on PR #41), and the consumers' actual current pins (they live in the Maskil / LH Worship Setlist / Versed repos, which are not reachable from here). PR #52's own CI (`Admission gauntlet` run 32884011446 on `21f4c7e`) completed 2026-08-25 18:48 UTC, conclusion failure, with exactly the pre-existing classes and nothing else: both `verify` legs red on the same single J39 test as main (`eval/test/gauntlet-machine-report.test.ts`, identical assertion at the identical line), the battery build leg red at "Fetch pinned sources (cache miss)" with byte-identical J52 checksum-mismatch lines, and the cross-leg-agreement cascade. The PR's own changes are green on both OSes — the full workbench suite passed (608 passed / 1 skipped on the ubuntu leg), including `compileJudgments.test.ts` (23 tests — one newly added by this PR; the file itself pre-exists on main). Two standing-down comments on the PR (2026-08-25) document the triage per failure class. On 2026-08-25 the PR #43 stack membership, the P5.6 sections-data attribution, the J31/J32 version attribution, and the full wrhubh-family branch accounting were re-verified against the bodies of PRs #43/#42/#35 (via the GitHub API), `git ls-remote --heads origin`, direct tree-hash comparisons (`7fa9110` ≡ `0326c6a`, both ≢ the current `-p2` tip `6f17ff6`; `6ffa8a2` ≡ `e01db27`), `engine/CHANGELOG.md`, and `docs/implementation-plan.md` §5. Also verified the same day, against the files at `origin/main` `caf9fe3` and the GitHub API: the battery-build "Committed descriptor matches the rebuilt artifact" gate and its error text (`.github/workflows/gauntlet.yml`), the mint's gate order (`.github/workflows/mint-artifact.yml`: "Validate the requested tag", then "Admission gauntlet" `--require-admit`, then "Fetch pinned sources"); the v2 approval schema's location and the 2026-08-20 v1 sunset (`docs/governance/probe-baseline-review.md` § "Approval schema v2 and the v1 grandfather"); PR #43's body text quoted for the #41 attribution and `b3f491d` in main's first-parent history for #38; run 32884011446's completed conclusion, its four failing jobs, and PR #52's two standing-down comments; and the FLAG-FOR-JESSE section heading in `search-quality-report-2026-08-20.md`.

**Re-verification snapshot, 2026-08-25 ~19:45 UTC** (a fresh `git fetch origin main` — main still at `caf9fe3` — plus the GitHub API and the workflow/script sources at that commit). Newly verified for this revision: **(1)** the changelog guard's exact behavior — `.github/scripts/changelog-guard.mjs`'s `checkChangelog` fails a pushed `v*` tag whose version ≠ `engine/package.json`'s version (message quoted verbatim in the Release runbook), run unconditionally by `release.yml`'s "Refuse to release an unchangelogged version" step; `engine/package.json` at `0.14.0` with a `## 0.14.0` CHANGELOG entry present — the basis for the v0.14.0 tag ruling and the stale-v0.9.0 warnings; **(2)** the battery-rebuild `release.tag` gap — `gauntlet.yml:437` passes `--out` and `--built-at` only (no `--release-tag`) to `build:artifact`, while `pipeline/src/buildArtifact.ts:581` emits `release: { tag }` only when the flag is present (argv-only, `:675`) — the basis of Known defects #5 and the corrected end-of-sequence claims (an earlier revision of this document asserted the descriptor PR alone turns battery green; that was wrong and is corrected throughout); the gate's full error text including the "(release-housekeeping 0.5-0.7)" suffix (`gauntlet.yml:446`); **(3)** GitHub state at 19:45 UTC — open PRs exactly #52 and #53 (both drafts; #53 created 2026-08-25 19:21:55 UTC, base `caf9fe3`, head `09e2794` on `claude/hearth-thread-wrhubh-j52`), 49 branch heads on origin including the new `claude/source-snapshots-2026-08-staging` (tip `1d7d2f4`); PR #53's full body read via the API — the source of the one-PR-supersedes-staged-runbooks framing, the blocked-on-upload condition, the fingerprint movement (`644b241c…` / `2b85fa88…`), the merge-base approval blobs, the subset-catch-up FINDING, and the merge-first-then-sign-J39-once recommendation this document adopts in the first-hour checklist; **(4)** the drift alarm's steady state — `pipeline/scripts/checkSourceDrift.ts` makes `DRIFTED` fatal with no rolling-source exemption (only the `contentSha256` repack path), and `pipeline/src/provenance/manifest.ts`'s `retrievalUrls` orders sourceUrl-then-archiveUrl (why the fetch stays green when the alarm re-fires); **(5)** PR #41's body section heading verbatim: "Ready-made review packet for Jesse". Everything stamped 19:45 UTC above is a point-in-time observation and can move again, exactly as the 18:48-era snapshot this revision corrects did.

**Targeted follow-up verification, 2026-08-25 ~20:15 UTC** (GitHub API job/step conclusions plus the run's downloaded logs): PR #53's own `Admission gauntlet` run **32889151337** on head `09e2794` (started 19:21:58 UTC, completed 2026-08-25 19:40:26 UTC, conclusion failure) — the run item (b) and first-hour step 3 cite as the pre-merge demonstration of the post-re-pin sequence. Its jobs: `battery (full artifact) — build` passed "Fetch pinned sources (cache miss)" (19:22:15–19:22:40) and "Build release artifact at the committed builtAt", then failed at "Committed descriptor matches the rebuilt artifact" (19:23:11) with the same "(release-housekeeping 0.5-0.7)" error text quoted in Known defects #5; both `verify` legs failed at "Unit tests" only, on the same single J39 canary as main's run 32881874670 (`eval/test/gauntlet-machine-report.test.ts`, "generates and verifies a real candidate-target report from exact descriptor and SQLite bytes" — 1 failed / 672 passed on each OS's eval suite); `battery (full artifact) — cross-leg agreement` failed at the first `download-artifact` step with everything downstream skipped.

**Update addendum, 2026-08-26 (post-PR-#52-merge; observed shortly after 01:22 UTC).** This revision adds only the dated PR #52 merge updates above (first-hour step 1, State of main, Branch dispositions, Known defects #2); nothing else was rewritten. Evidence, all via the GitHub API plus a fresh `git fetch origin main`: PR #52 reports `merged: true`, `merged_at` 2026-08-26T01:22:52Z, `merged_by` jestek-dev; the squash merge commit is `031ed69b5c4be84bdf4bd3b03e9b589891ad82bf` ("Fix compileJudgments crash on verse-level web-subset selections (#52)"), which is `origin/main`'s new HEAD. Open PRs are now exactly one: #53, still a draft (base `caf9fe3`, head `09e2794`, unchanged). The post-merge `Admission gauntlet` run on main — **32918755741** on `031ed69`, started 2026-08-26T01:22:55Z — was **still in_progress at observation time, so its conclusion is not yet verified**; the expected outcome is the same two pre-existing red classes as run 32881874670 (the J39 canary on both `verify` legs and the J52 battery fetch/cascade), since the merged change is workbench tooling only, but confirm against the completed run rather than trusting this expectation.

**Update addendum, 2026-08-26 ~10:55 UTC (the #54–#59 concept-pack stack).** This revision adds only the dated stack updates (first-hour step 6, the State-of-main second update, the Deferred-roadmap disposition, the open-decisions and Branch-dispositions entries, and the Deliverables-map `engine-pack-backlog.md` line); no stamped claim was rewritten. Evidence, all via the GitHub API at ~10:55 UTC plus the files on the share: **(1)** open PRs are exactly seven, all drafts — #53 (unchanged) and #54–#59, verified individually open + draft with a consistent stack topology: #54 base `main` / head `claude/hearth-161-concept-packs-2tf8jk` @ `c9b3f57`; #55 base `…-2tf8jk` / head `…-b2` @ `2d55955`; #56 base `…-b2` / head `…-b3` @ `adc1280`; #57 base `…-b3` / head `…-b4` @ `73278bb`; #58 base `…-b4` / head `…-b5` @ `c461559`; #59 base `…-b5` / head `…-b6` @ `25297ab` — matching the backlog file's own table sha-for-sha (its three "inferred" heads confirmed). **(2)** `origin/main` HEAD still `031ed69` — unmoved since the PR #52 merge. **(3)** The census and no-junk claims are cited from the PR bodies, not recounted: #54's body states 153 → 168 ("all 168 concepts demonstrated", G4 "168 concepts mutually distinct") and "Zero NO MEASURABLE EFFECT verdicts"; #59's body states "Concept census 223 → 239", "all 239 concepts demonstrated", and "This completes the 161-row review: every row is now dispositioned — the residual roster is the corpus-blocked deferral list riding the PR #53 re-pin". **(4)** The standing-down CI triage: #58's comment (2026-08-26 09:27 UTC) was read in full — a four-row table matching run 32943823904's reds to the base run's pre-existing J39/J52 signatures exactly ("No red is introduced by this PR… Standing down"); #54 and #59 each carry exactly one comment of the same series. Note the backlog file's header lists standing-down comments on "#54, #55, #56, #57, #59" with batch 6/#58 covered by a ledger note — that list predates #58's comment, which does exist; the file is otherwise consistent with everything observed. **(5)** `/mnt/project-files/research/bible-rollout/engine-pack-backlog.md` exists (mtime 2026-08-26 10:52; ~20.6 KB); its header and roster confirm: faithful transcription of the rollout ledger, the six-PR table above, "Zero `NO MEASURABLE EFFECT` packs were merged", the 50-row corpus-blocked roster ("49 review rows + the standing `virgin-birth` item", with the folded `waiting-and-timing-in-love` riding row 48), and the § "Items flagged for Jesse's decision". Not verified here: the per-batch admitted/deferred arithmetic inside the ledger (transcribed, per its own header, not re-judged) and #55–#57's comment texts individually (counts and the backlog's record only). All 10:55 UTC facts are point-in-time snapshots.

**Update addendum, 2026-08-26 ~22:09 UTC (the PR #53 merge, the #55–#59 mis-target and #60 recovery, draft PR #61).** This revision adds only the dated updates above (first-hour steps 3–6, State of main, CI item (b), the open-decisions and Deferred-roadmap stack entries, Branch dispositions, Known defects #5); no stamped claim was rewritten. Evidence, all via the GitHub API plus a fresh `git fetch origin main`: **(1)** PR #53 reports `merged: true`, `merged_at` 2026-08-26T15:22:04Z, `merged_by` jestek-dev, base at merge `6367855`, head `e446347…`; the squash commit `e762d1c629f5b121a2aacc6da57cca6bacc3215e` (verified single-parent on `6367855`) is `origin/main`'s HEAD. Its body's own 2026-08-26 update section is the source for: release `source-snapshots-2026-08` published 14:47 UTC by jestek-dev with all three asset digests verified against the staging `CHECKSUMS.txt` (`b6f55cc7…` / `fee5234e…` / `2006d1af…`), the concept-pack merge into the branch (merge commit `e446347`, baselines regenerated with sanctioned flags only, approvals untouched), the final identity 0.14.0 / `644b241c…` / `b24ea16d304e062f2f4c2a8fd6d55a9727df8b4240b8df5a802cd217c3bbd2a3`, the updated merge-base approval blobs (main @ `6367855`: `ordering.snapshot.json` @ `f534b863…`, `probes.json` @ `8b9d774a…`), and A5b remaining open. **(2)** The stack: #54 merged 14:18:46 UTC with base `main` (squash `8c8b78d`); #55–#59 merged 14:19–14:21 UTC with their bases still the predecessor batch branches (verified per-PR — none was retargeted, so none landed on main); recovery PR #60 (created 14:36 UTC, base main @ `8c8b78d`, head `…-b6` @ `2e9256b`, +16,369/−930 across 198 files) merged by jestek-dev at 14:53:46 UTC as `6367855`; its body carries the mis-target ledger quoted in Branch dispositions and states the diff vs main "is precisely the sum of the five merged PRs' diffs — nothing new". **(3)** Open PRs at 22:09 UTC: exactly one — **#61**, draft, opened 22:06:34 UTC by claude[bot], head `claude/hearth-wrapup-handoff-27it4g` @ `39b562a`, base `main` @ `e762d1c`; its diff was read in full (one file, `.github/workflows/gauntlet.yml`: the `RELEASE_TAG` read-back from the committed descriptor, appended only when non-empty, plus an explanatory comment block — matching Known defects #5's prescribed change); the API shows **no reviews and no comments** on it. **(4)** Gauntlet runs on main: run **32918755741** on `031ed69` — the run the 01:22 addendum left in_progress — completed 01:40:07 UTC, conclusion failure, with exactly the expected pre-existing classes and nothing else (both `verify` legs failed at "Unit tests" only; battery build failed at "Fetch pinned sources (cache miss)" with everything downstream skipped; cross-leg failed at the first `download-artifact`) — that addendum's expectation is now confirmed. Runs 32979500945 (`8c8b78d`) and 32983128553 (`6367855`) both completed with conclusion failure (conclusions only; jobs not inspected here). Run **32984930946** on `e762d1c` (push, run number 155): created 15:23:19 UTC and **still status `queued` with no jobs started at 22:09 UTC** — nearly seven hours in queue; its conclusion is unverified, so the post-re-pin shape on main (fetch green; descriptor gate + J39 legs red) remains the predicted/pre-merge-observed sequence, not a main observation. All 22:09 UTC facts are point-in-time snapshots.

**Update addendum, 2026-08-26 (pastoral-* id split).** This revision adds only the "Known behavior (not a defect)" subsection under Known defects; no stamped claim was rewritten. Evidence: the 14 `pastoral-*.yaml` `id:` fields read via `git show` at `origin/main` `e762d1c`; `ontologyImporter.ts:111-120` and `buildConceptLayer.ts:176-177` at the same commit; a local `npm run build:fixture` from a worktree verified byte-identical to `e762d1c` for `ontology/`+`pipeline/`+`eval/` (`git diff --stat` empty), whose final layer fingerprint `b24ea16d…` matches the post-#53 identity, then a direct `node:sqlite` query of `pipeline/output/fixture.db`'s `concepts` table (0 ids matching `%pastoral%`, all 14 unprefixed ids present, 239 rows); CONVENTIONS.md §5 read on the share; book-doc tags spot-checked in `1-samuel.md` and `2-corinthians.md`.

**Update addendum, 2026-08-27 (the refresh — the current state of the project).** This revision adds the banner, four inline [Superseded 2026-08-27 →] markers, and this addendum; no stamped claim was rewritten. Evidence: `git log` on `origin/main` (tip verified `65b6a6f`), the team-memory record exported at `project-handoff/memory-export/`, and the delivered plan/ruling files copied under `project-handoff/project-files/`.

**What landed after this document's last (2026-08-26 ~22:09 UTC) addendum — seven merges, in order:**

1. **PR #64, merged 2026-08-27 01:36 UTC (squash `9099890`)** — the full-Bible fixture corpus expansion (PR-β): **5,726 → 31,098 verses / 1,189 chapters**, executing the 32-row combined corpus-expansion + xref ruling Jesse approved 2026-08-26 21:58 UTC ("Agree with all"). `corpusFingerprint` moved to **`6450b7d7…`**; no ENGINE_VERSION bump. The CI fixture corpus now measures the whole Bible.
2. **PR #62, merged 01:37 UTC (`bd883db`)** — four passing pending fixtures promoted to active (G3 recommendation).
3. **PR #63, merged 03:28 UTC (`6686b55`)** — popular-verses Tiers 1–3 promoted to golden fixtures: **all 103 guard fixtures active and measured** (whole-Bible-plan §4.2; the post-#64 refresh promoted the 10 previously out-of-corpus verses).
4. **PR #66, merged 03:30 UTC (`0d12c34`)** — six more pending fixtures promoted (including the it-is-well/Jer 4:10 guard — the last harmful result from the 2026-08-20 battery audit, closed).
5. **PR #65, merged ~13:02 UTC (`b81f17c`)** — alias/lexicon mining batch 1: 5 PD remembered-phrasing packs + 1 extension (cast-your-care → 1 Pet 5:7, no-weapon-shall-prosper → Isa 54:17, wait-for-the-lord → Isa 40:31, the-cross ext → Isa 53:5, mind-stayed-on-thee → Isa 26:3, Rom 4:17 with the J9 slogan guard), all previous misses now #1; PD wording only per the standing ruling.
6. **PR #67, merged 13:44 UTC (squash `65b6a6f` = main tip)** — the corpus-blocked roster build: **47 of 50 rows executed (44 new packs + 3 lexicon extensions), fixtures-first, zero NO-MEASURABLE-EFFECT**. Verified on main: **288 concept files** (census test asserts 288), **530 active golden fixtures all holding**. The 3 unexecuted rows are ruling-gated: `end-times` (day-of-the-lord merge), `exile-and-captivity` (routing), `virgin-birth` (supplement).
7. Between #63 and #65, the three commissioned plan deliverables landed on the share (all copied into this folder): the **44-call theology-rulings ledger** (`project-files/rulings/2026-08-27-theology-rulings-ledger.md`, awaiting Jesse's reply-by-number — rows 1–4 gate v0.14.0), the **sweep-adjudication plan** (`project-files/plans/2026-08-27-sweep-adjudication-plan.md`, ~3,600 sweep candidates → an honest 15–25 reviewed PRs), and the **votes-to-engine plan** (`project-files/plans/2026-08-27-votes-to-engine-plan.md`, whose open calls Jesse ruled 13:25 UTC — all defaults accepted; call #4, the post-wind-down J39 signer, remains open by design).

**Identity and J39 — the headline.** With #67 merged, **no open identity movers remain** (the only open PR is draft **#61**, which touches only `.github/workflows/gauntlet.yml`). The identity the J39 approvals must bind is final: **engine 0.14.0 / corpus `6450b7d7…` / layer `9a11fd56edd7511bc7139db642dee26c19e5d4ce4456ed11bdc99b8bf181b8c5`** (the layer is the union of #65's `1a3516ba…` and the roster's `e4f864bf…`). **J39 signing is FULLY UNBLOCKED — sign once, against this identity.** The refreshed final walkthrough was posted to the project thread ~14:00 UTC 2026-08-27: digests recomputed and a full dry-run validated against `65b6a6f` — **reviewPacketSha256 `879e1db9…`**, probes/ordering digests unchanged from the pre-merge computation, dry-run verdict **ADMIT WITH WARNINGS** (G2: 25 orderings byte-identical; G8: 0% churn; G3: a single WARN on pending fixtures — see the cosmetic defect below). This supersedes every earlier fingerprint pair quoted in the body (`644b241c…`/`2b85fa88…` and `644b241c…`/`b24ea16d…` were both real at their stamps and are both obsolete).

**One new (cosmetic) gauntlet defect, flagged to Jesse with the walkthrough:** the G3 summary **names 17 pending fixtures but counts 16/16** — `lords-supper-verbatim` is listed but uncounted. Cosmetic (no verdict impact), but it belongs on this document's defect list as #7.

**The remaining path, in order:**

1. **J39 — sign once** (reviewer designated by Jesse; two v2 approval records binding the identity above; landed via a reviewed PR). Turns both `verify` legs green.
2. **Branch cleanup** — deletable on recorded evidence: `-p7`, `-p8`, `source-snapshots-2026-08-staging`, `-j52`, `-fixture-promotions`, `-fixture-promotions-2`, and the `hearth-161-concept-packs-2tf8jk`…`-b6` family; formal approval is the J62-class call; `claude/hearth-thread-vvwdi2` stays until J61 is decided.
3. **v0.14.0 mint → descriptor PR → tag** per the Release runbook above, whose three known blockers all stand: **(a)** the corpus-expansion **supplement ruling S1–S6** (+ virgin-birth venue + row-20 shape — `project-files/research/bible-rollout/corpus-expansion-ruling-supplement.md`, ~6 minutes of reply, blocks the mint), **(b)** the **G10 size call** (167.84 MiB vs 160 MiB, defect #6 — reviewed `eval/budgets.json` decision before/alongside the descriptor PR), and **(c)** defect #5's `--release-tag` gap, whose fix is **draft PR #61** (merge after the mint's descriptor PR to let the battery legs, and main, go fully green).

Everything else open — Jesse's calls and successor work alike — is enumerated with verification notes in **`project-handoff/OUTSTANDING.md`**; the dated narrative of how the project got here is **`project-handoff/HISTORY.md`**; the complete document catalog is **`project-handoff/INDEX.md`**. All 2026-08-27 facts above are point-in-time observations, stamped as such.
