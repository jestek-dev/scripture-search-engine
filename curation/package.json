{
  "name": "@jestek-dev/scripture-curation",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Offline embedding-assisted curation tooling (plan P4.16 / borrowable B4). Proposes candidates and flags sense inversions for HUMAN review; nothing here is part of the artifact build graph, and nothing here ships. The runtime is statistics and lookups — covenant #1 is the design law.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "fetch-model": "node ../node_modules/tsx/dist/cli.mjs src/fetchModel.ts",
    "suggest": "node ../node_modules/tsx/dist/cli.mjs src/suggest.ts",
    "flag-inversions": "node ../node_modules/tsx/dist/cli.mjs src/flagInversions.ts",
    "test": "node ../node_modules/vitest/vitest.mjs run"
  },
  "devDependencies": {
    "@xenova/transformers": "2.17.2"
  }
}
