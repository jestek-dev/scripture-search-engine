{
  "name": "scripture-search-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Deterministic, concept-aware Scripture search shared by Maskil, LH Worship Setlist, and Versed. No runtime AI.",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "engine",
    "artifact-client",
    "conformance",
    "pipeline",
    "eval",
    "workbench",
    "sweep"
  ],
  "scripts": {
    "build:engine": "npm run build --workspace engine",
    "typecheck": "npm run typecheck --workspaces --if-present && npm run typecheck --prefix curation",
    "test": "npm run test --workspaces --if-present",
    "check:drift": "npm run check:drift --workspace pipeline",
    "gauntlet": "npm run gauntlet --workspace eval --",
    "gauntlet:report": "npm run gauntlet -- --require-admit --json eval/.runs/gauntlet-report.json",
    "verify": "npm run build:engine && npm run typecheck && npm run test && npm run gauntlet --"
  },
  "devDependencies": {
    "tsx": "4.23.1",
    "typescript": "5.9.3",
    "vitest": "4.1.10"
  }
}
