id: peace-of-god
label: Peace of God
# Torrey/Miller admission 2026-08-06. Torrey anchors drawn from the verified
# outline(s): CARE, OVERMUCH; PEACE, SPIRITUAL. Lexicon is modern
# editorial phrasing; Torrey headings are provenance, not query language.
lexicon:
  - peace of god
  - peace that passes understanding
  - perfect peace
  - do not be anxious
  - peace i leave with you
  # Single-token entry, added 2026-08-08. Bare words are what people actually
  # type; the one-token specificity discount in conceptAnchorEvidence (0.55)
  # keeps the claim humble relative to a multi-word match.
  - peace
  # Remembered-phrasing entry, 2026-08-08. The wording people search with is
  # NIV/ESV; the corpus is WEB, so this phrasing reached nothing before.
  # Measured gap — see docs/research/2026-08-08-remembered-phrasings.md.
  - cast all your anxiety on him
  # Inner-life aliases, 2026-08-18 (books-harvest curation). Bare words per
  # the approved "anxiety" precedent (§1.6d option 3 /
  # remembered-anxious-for-nothing): the words people actually type, kept
  # humble by the one-token discount. Routed HERE and not to
  # remembered-anxious-for-nothing because the worry answer set is this
  # concept's anchor set (Phil 4:6-7; 1 Pet 5:7, whose WEB text reads
  # "casting all your worries on him"; Matt 6:25-27; John 16:33); the
  # remembered pack keeps its narrow one-anchor design. Landed together with
  # the 1 Peter 5:7 dedup so the added traffic never sees the double-count.
  # "worried" is a separate entry because the stemmer yields a different
  # token than "worry"; "worries"/"worrying" normalize to "worry" and
  # "stressed" to "stress", so neither needs an entry.
  - worry
  - worried
  - do not worry
  - stress
  # Round-2 book admission 2026-08-18.
  - let the peace of god rule
  - do not fret
  # The Phil 4:8 thought-life phrasings, routed HERE per the Psalm-91
  # precedent: this concept already owns the passage (Phil 4:6-7 at 1.0),
  # and the anchor below carries vv8-9 to its "the God of peace will be
  # with you" close. The remembered-transformed-not-conformed pack keeps
  # its narrow one-anchor design (integrator call; author F had proposed
  # Phil 4:8 there).
  - whatever is true
  - think about such things
anchors:
  - ref: Philippians 4:6-7
    sources: [editorial]
    weight: 1.0
  - ref: John 14:27
    sources: [editorial]
    weight: 0.95
  - ref: Isaiah 26:3
    sources: [editorial]
    weight: 0.9
  - ref: John 16:33
    sources: [torrey]
    weight: 0.8
  # Deduplicated 2026-08-18: this concept listed 1 Peter 5:7 twice (torrey
  # 0.75 + a remembered-phrasing editorial entry at 0.85, added 2026-08-08 —
  # "cast all your anxiety on him" is this verse), and the engine sums
  # duplicate anchors, so the verse outscored Philippians 4:6-7 for "peace".
  # The torrey entry survives with its original weight; the remembered-
  # phrasing rationale now lives on this comment. Dual citation
  # `sources: [torrey, editorial]` should be restored once multi-source
  # entries are single-scoring (2026-08-14 plan, ranking-fixes §6 #3).
  - ref: 1 Peter 5:7
    sources: [torrey]
    weight: 0.75
  - ref: Matthew 6:25-27
    sources: [torrey]
    weight: 0.7
  - ref: Romans 5:1
    sources: [torrey]
    weight: 0.6
  # Round-2 book admission 2026-08-18 — leads from the approved books,
  # verified in WEB context; the books are never engine sources.
  # "let the peace of God rule in your hearts." Torrey: PEACE, SPIRITUAL.
  - ref: Colossians 3:15
    sources: [torrey]
    weight: 0.85
  # "whatever things are true... think about these things. ...and the God
  # of peace will be with you" — contiguous with the pack's Phil 4:6-7
  # lead (collapseAnchorRuns applies); the peace promise closes the unit.
  # SIBLING NOTE (tag-gap batch 4, 2026-08-26): thought-life claims v8
  # alone for the what-to-think-about register (recorded in both files);
  # this pack keeps the whole span for the anxiety register.
  - ref: Philippians 4:8-9
    sources: [editorial]
    weight: 0.8
  # "Rest in the LORD... Don't fret."
  - ref: Psalms 37:7
    sources: [editorial]
    weight: 0.7
  # "Don't fret because of evildoers."
  - ref: Psalms 37:1
    sources: [editorial]
    weight: 0.6
related:
  - trust-in-god
  - fear-not
